---
phase: 15-saved-lineups-firestore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/firebase.js
  - src/MadeiraLineupPlanner.jsx
autonomous: true
requirements:
  - SAVE-01
  - SAVE-02
  - SAVE-03
  - SAVE-04

must_haves:
  truths:
    - "Saved lineups persist in a Firestore collection `savedLineups` (durable across Safari/iOS localStorage ITP wipes)"
    - "On first post-upgrade load, any existing localStorage `madeira_savedLineups` entries that don't yet have a Firestore id are pushed to Firestore; a `madeira_savedLineups_migrated` flag is then set so migration runs exactly once per device"
    - "Save, edit, delete operations write to Firestore AND update the localStorage cache"
    - "Initial app load renders from localStorage immediately (fast first paint), then reconciles with Firestore in the background"
    - "If Firestore is unavailable (offline, error), local saves still work and reconcile on next load"
  artifacts:
    - path: src/firebase.js
      provides: "savedLineups CRUD + listSavedLineups"
      contains: "savedLineupsCol"
    - path: src/MadeiraLineupPlanner.jsx
      provides: "Migration + reconciliation + write-through on save/delete"
      contains: "savedLineups_migrated"
---

<objective>
Close SAVE-01..04. Saved lineups survive Safari/iOS ITP localStorage wipes by persisting to a new Firestore collection, with localStorage retained as a read-through cache for instant first paint and offline resilience. Existing local-only entries migrate in a single one-shot operation.
</objective>

<context>
**Current state:** `savedLineups` lives only in React state + `madeira_savedLineups` localStorage. Each entry is a snapshot object with `{name, formation, lineup, inactiveIds, roster, date}`. `saveLineup` pushes a new entry. `loadLineup(index)` / `deleteLineup(index)` use the array index.

**New Firestore layout:** collection `savedLineups`, each doc with the snapshot shape plus `savedAt` timestamp. Docs keyed by auto-generated Firestore ids.

**Migration flag:** `madeira_savedLineups_migrated` — set to `true` after the first successful push of local-only entries. Prevents re-pushing on subsequent loads.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firestore CRUD for savedLineups</name>
  <files>src/firebase.js</files>
  <action>
    Add at the end of the file (keep organized under a "Saved Lineups" section comment):
    - `const savedLineupsCol = collection(db, "savedLineups");`
    - `export async function listSavedLineups()` — fetch all docs, map to `{id, ...data}`, sort by `savedAt` descending (fallback to the legacy `date` field). On error return [].
    - `export async function createSavedLineup(snapshot)` — `addDoc(savedLineupsCol, {...snapshot, savedAt: new Date().toISOString()})`. Returns the new doc id, or null on failure.
    - `export async function deleteSavedLineup(id)` — `deleteDoc(doc(db, "savedLineups", id))`. Returns boolean.

    Use existing imports (collection, addDoc, deleteDoc, doc, getDocs).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
    <automated>grep -n "savedLineupsCol\|listSavedLineups\|createSavedLineup\|deleteSavedLineup" src/firebase.js</automated>
  </verify>
  <done>CRUD helpers exist, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migration + reconciliation in MadeiraLineupPlanner</name>
  <files>src/MadeiraLineupPlanner.jsx</files>
  <action>
    1. Import `listSavedLineups`, `createSavedLineup`, `deleteSavedLineup` from `./firebase`.

    2. On mount, run an async effect:
       - Show local cache immediately (already done by `useState(() => loadStored("savedLineups", []))`)
       - Call `listSavedLineups()`
       - Read `loadStored("savedLineups_migrated", false)`
       - If NOT migrated: find local entries without `id`; for each, call `createSavedLineup(entry)` → get id → attach; write `saveStored("savedLineups_migrated", true)`; then `listSavedLineups()` again to pick up the migrated entries with their ids
       - If already migrated: remote is the source of truth
       - Set `savedLineups` state to the remote array; write the same to localStorage cache

    3. Change `saveLineup(name)`:
       - Build snapshot (existing code)
       - Call `createSavedLineup(snapshot)` → get id
       - If success: push `{...snapshot, id}` to state + cache; toast "Lineup saved"
       - If failure: push snapshot without id to state + cache; toast "Saved locally (cloud sync failed)" — local-only entries will migrate on the next successful load

    4. Change `deleteLineup(index)`:
       - Grab `savedLineups[index].id`
       - If has id, fire `deleteSavedLineup(id)` (fire-and-forget; log on failure)
       - Remove from state + cache (always, regardless of cloud success)

    5. Keep the existing `savePublishedLineup(...)` call inside `saveLineup` — that's a separate concern (publishing the currently-edited lineup to the live `lineups/published` doc).

    6. `loadLineup(index)` unchanged — it only reads from state.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
    <automated>grep -n "savedLineups_migrated\|createSavedLineup\|deleteSavedLineup" src/MadeiraLineupPlanner.jsx</automated>
  </verify>
  <done>Migration + reconciliation work. Builds pass.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npx vitest run` exits 0 (no regressions)
- SAVE-01..04 all covered

Manual testing deferred to end-of-v2.1 review per session instruction.
</verification>

<success_criteria>
- Existing local entries auto-migrate once and then survive localStorage wipes
- New saves hit Firestore + cache
- Deletes remove from both Firestore + cache
- App starts instantly from localStorage cache, reconciles in background
</success_criteria>

<output>
After completion, create `.planning/phases/15-saved-lineups-firestore/15-01-SUMMARY.md`
</output>
