---
phase: 08-config-layer-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/firebase.js
  - .env.local
  - .env.example
  - .gitignore
  - src/tests/config.test.js
autonomous: false
requirements:
  - CFG-01
  - CFG-05

must_haves:
  truths:
    - "src/firebase.js reads apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId, measurementId from src/config.js (not inline literals)"
    - "src/config.js reads Firebase values from import.meta.env.VITE_FIREBASE_* at build time"
    - "src/config.js exposes a GAME_STRUCTURE constant resolved from import.meta.env.VITE_GAME_STRUCTURE, defaulting to 'halves' when unset"
    - "A Madeira dev build started with the populated .env.local connects to the existing madeira-fc-lineups Firebase project with zero behavior change"
    - "A vitest suite in src/tests/config.test.js asserts DEPLOYMENT config shape and GAME_STRUCTURE normalization"
  artifacts:
    - path: "src/config.js"
      provides: "Build-time deployment config module — single source for Firebase creds, team name (stub for 08-02), roster (stub for 08-03), formations (stub for 08-03), game structure"
      exports: ["DEPLOYMENT", "FIREBASE_CONFIG", "GAME_STRUCTURE"]
      min_lines: 40
    - path: ".env.local"
      provides: "Madeira deployment env file with VITE_FIREBASE_* values matching the current hardcoded config and VITE_GAME_STRUCTURE=halves"
    - path: ".env.example"
      provides: "Checked-in template showing every VITE_* key a deployment needs (no secrets)"
    - path: ".gitignore"
      provides: "Ignores .env.local, .env.*.local so real credentials never ship"
      contains: ".env.local"
    - path: "src/tests/config.test.js"
      provides: "Unit tests covering DEPLOYMENT shape and GAME_STRUCTURE default/normalization"
      min_lines: 30
  key_links:
    - from: "src/firebase.js"
      to: "src/config.js"
      via: "import { FIREBASE_CONFIG } from './config'"
      pattern: "import.*FIREBASE_CONFIG.*from.*['\"]\\./config['\"]"
    - from: "src/config.js"
      to: "import.meta.env"
      via: "Vite build-time env var substitution"
      pattern: "import\\.meta\\.env\\.VITE_FIREBASE_API_KEY"
    - from: ".gitignore"
      to: ".env.local"
      via: "prevents credential leak"
      pattern: "\\.env\\.local"
---

<objective>
Create the per-deployment config module and wire Firebase to read from it via Vite env vars. This is the foundation every later Phase 8 plan builds on — team name, roster, formations, and game structure all get hung off this same `src/config.js` module in subsequent plans.

Purpose: Removes the hardcoded Firebase credentials from src/firebase.js (CFG-01) and introduces the GAME_STRUCTURE config slot (CFG-05), establishing the env-var-driven pattern that CFG-02..CFG-04 plug into next.
Output: `src/config.js`, `.env.local` (Madeira values, gitignored), `.env.example` (template, checked in), updated `src/firebase.js`, updated `.gitignore`, `src/tests/config.test.js`.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

@src/firebase.js
@src/shared/constants.js
@vite.config.js
@package.json

<interfaces>
<!-- Existing hardcoded Firebase config that must be migrated verbatim to .env.local -->

From src/firebase.js (lines 23-31, to be replaced):
```javascript
const firebaseConfig = {
  apiKey: "AIzaSyAH8wCDYl7d751nr-mpW6_WOGJfIRtyXmI",
  authDomain: "madeira-fc-lineups.firebaseapp.com",
  projectId: "madeira-fc-lineups",
  storageBucket: "madeira-fc-lineups.firebasestorage.app",
  messagingSenderId: "275318113105",
  appId: "1:275318113105:web:10c66108571df4a1588970",
  measurementId: "G-3XPY82HK3K",
};
```

Vite env contract (from vite.config.js + Vite docs):
- Only keys prefixed `VITE_` are exposed to client code via `import.meta.env.VITE_*`
- `.env.local` is auto-loaded for `vite dev` and `vite build` in the current working dir
- Values are inlined at build time (no runtime fetch needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config module + env files + tests</name>
  <files>
    src/config.js,
    .env.local,
    .env.example,
    .gitignore,
    src/tests/config.test.js
  </files>
  <behavior>
    - Test 1: `FIREBASE_CONFIG` is an object with all 7 keys (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId, measurementId) and each value is a non-empty string when VITE_FIREBASE_* vars are set.
    - Test 2: `GAME_STRUCTURE` equals the string "halves" when VITE_GAME_STRUCTURE is "halves".
    - Test 3: `GAME_STRUCTURE` equals "quarters" when VITE_GAME_STRUCTURE is "quarters".
    - Test 4: `GAME_STRUCTURE` falls back to "halves" when VITE_GAME_STRUCTURE is unset/empty.
    - Test 5: `GAME_STRUCTURE` throws on an explicit invalid value (e.g., "thirds") — document and assert the chosen behavior.
    - Test 6: `DEPLOYMENT` is the exported umbrella object with `firebase` (FIREBASE_CONFIG) and `gameStructure` (GAME_STRUCTURE) keys, plus reserved placeholder keys `teamName`, `roster`, `formations` set to undefined (filled by 08-02 / 08-03).
  </behavior>
  <action>
    1. Create `src/config.js`:
       - Read each Firebase field via `import.meta.env.VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID`, `VITE_FIREBASE_MEASUREMENT_ID`.
       - Export named `FIREBASE_CONFIG` (object with apiKey/authDomain/etc).
       - Read `VITE_GAME_STRUCTURE`; normalize to lowercase; validate one of `["halves", "quarters"]`; if unset default to `"halves"`; if invalid throw `Error("Invalid VITE_GAME_STRUCTURE: expected 'halves' or 'quarters', got <value>")`.
       - Export named `GAME_STRUCTURE`.
       - Export default and named `DEPLOYMENT = { firebase: FIREBASE_CONFIG, gameStructure: GAME_STRUCTURE, teamName: undefined, roster: undefined, formations: undefined }` — the undefined slots are placeholders so plan 08-02 / 08-03 can fill them without restructuring imports.
       - Add a top-of-file comment block listing every required env var with a one-line description — this doubles as inline docs for `/gsd:plan-phase 11` when it writes DEPLOYMENT.md.
    2. Create `.env.local` at repo root (gitignored) with the existing Madeira values verbatim from the interfaces block above, prefixed `VITE_FIREBASE_*`. Also set `VITE_GAME_STRUCTURE=halves` and `VITE_DEPLOYMENT_ID=madeira`. DO NOT commit this file.
    3. Create `.env.example` at repo root (checked in) — same keys as `.env.local` but with placeholder values like `your-api-key-here`, `your-project.firebaseapp.com`, `VITE_GAME_STRUCTURE=halves|quarters`. Include a header comment explaining what each deployment needs to fill in.
    4. Append to `.gitignore` (read first, don't duplicate):
       ```
       # Per-deployment secrets
       .env.local
       .env.*.local
       ```
       Do NOT add `.env.example` to the ignore list — it must be committed.
    5. Create `src/tests/config.test.js` with the tests described in `<behavior>`. Use `vi.stubEnv("VITE_FIREBASE_API_KEY", "...")` etc. and `vi.unstubAllEnvs()` between tests to control the env values under test. Reset module cache between tests (`vi.resetModules()`) since config.js reads env at import time.
    6. Run `npx vitest run` — all new tests must pass; existing tests must still pass.
    7. Modify `src/firebase.js`:
       - Remove the inline `firebaseConfig` object (lines 23-31, including the TODO comment block).
       - Add `import { FIREBASE_CONFIG } from "./config";` at the top.
       - Replace `const app = initializeApp(firebaseConfig);` with `const app = initializeApp(FIREBASE_CONFIG);`.
    8. Run `npm run build` — must succeed with zero errors.
    9. Do NOT modify team name, roster, formations, or any rendering code in this plan. Those belong to 08-02 and 08-03.

    AVOID:
    - Do NOT read env vars anywhere outside src/config.js — that's the whole point of centralizing.
    - Do NOT put real credentials in .env.example (checked in).
    - Do NOT use `process.env` — Vite uses `import.meta.env`.
    - Do NOT rename existing env var keys (you'll only confuse Phase 11 when DEPLOYMENT.md gets written).
  </action>
  <verify>
    <automated>npx vitest run src/tests/config.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - src/config.js exists and exports FIREBASE_CONFIG, GAME_STRUCTURE, DEPLOYMENT
    - src/firebase.js has no inline Firebase credentials; only imports from ./config
    - `grep -n "AIzaSy" src/` returns zero hits (no inline Firebase API key anywhere in src/)
    - .env.local exists with all 7 VITE_FIREBASE_* vars + VITE_GAME_STRUCTURE=halves + VITE_DEPLOYMENT_ID=madeira
    - .env.example checked in with placeholder values
    - .gitignore contains `.env.local`
    - src/tests/config.test.js passes with ≥ 5 tests
    - `npm run build` succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Madeira dev run loads from config with zero behavior change</name>
  <files>(no files modified — human runs dev server and inspects UI)</files>
  <action>
    **Human verification step.** Claude should run `npm run dev` in a background terminal so the user can navigate to the running app, then pause until the user confirms. No source files are modified in this task. If the user reports a regression, fix it before proceeding to Plan 08-02.

    Context for the user:
    src/config.js centralizes all build-time deployment config. src/firebase.js now reads Firebase credentials from .env.local via config.js. GAME_STRUCTURE is available (defaults "halves") but not yet consumed anywhere. Madeira should look and behave identically to before this plan.

    Steps for the user to run:
    1. `npm run dev` in one terminal (Claude can start this in the background).
    2. Open http://localhost:5173 in a browser.
    3. The Lineup tab should load with the existing 13-player Madeira roster (from localStorage; if localStorage is empty, from the still-hardcoded INITIAL_ROSTER — CFG-03 swap comes in 08-03).
    4. DevTools → Network → verify Firestore reads hit `firestore.googleapis.com` for project `madeira-fc-lineups` — confirms Firebase config loaded from .env.local.
    5. DevTools → Application → confirm data still loads (published lineup, past games list on Games tab).
    6. Create one test game on the Games tab, then delete it; confirm Firestore read/write works end-to-end.
    7. In the browser console, evaluate `import.meta.env.VITE_GAME_STRUCTURE` (or log `DEPLOYMENT.gameStructure` from a dev-mode inspection) — should read "halves".
    8. Stop the dev server, temporarily rename `.env.local` → `.env.local.bak`, run `npm run dev` again — app should fail to boot (or Firestore calls should fail) because credentials are missing. Restore `.env.local`.

    Expected: Every Madeira flow works identically to before the plan. No visual diffs. No new console errors except the intentional negative test in step 8.

    Resume signal: User types "approved" if Madeira behavior is unchanged, or describes any regression. Claude must NOT proceed to Plan 08-02 until approval.
  </action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint. Automated verify in Task 1 covers tests + build; this task verifies UI parity which cannot be automated without a visual regression harness (out of scope for Phase 8).</automated>
  </verify>
  <done>
    - User has run the Madeira dev server with the new config-driven Firebase setup
    - User has confirmed every Madeira surface renders and behaves identically to pre-plan
    - User has typed "approved" (or the equivalent) to unblock Plan 08-02
  </done>
</task>

</tasks>

<verification>
- Automated: `npx vitest run && npm run build` both pass
- Grep: `grep -rn "AIzaSy\|madeira-fc-lineups.firebaseapp.com\|275318113105" src/` returns zero hits (all Madeira-specific secret-like literals removed from src/)
- Manual (checkpoint): Madeira dev server loads, reads/writes Firestore, renders identically
</verification>

<success_criteria>
- CFG-01: Firebase connection loaded from environment variables via src/config.js → src/firebase.js
- CFG-05: GAME_STRUCTURE constant exported from src/config.js; Madeira's .env.local sets it to "halves"
- Existing Madeira instance runs unchanged (same Firebase project, same data, same UI)
- Every subsequent Phase 8 plan can plug teamName/roster/formations into DEPLOYMENT without restructuring imports
</success_criteria>

<output>
After completion, create `.planning/phases/08-config-layer-extraction/08-01-SUMMARY.md` per the summary template. Include: env var keys introduced, the DEPLOYMENT shape (so 08-02 and 08-03 executors don't have to re-read source), and any gotchas (e.g., if Vite's env handling surprised you).
</output>
