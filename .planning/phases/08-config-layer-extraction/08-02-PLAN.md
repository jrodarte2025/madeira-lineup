---
phase: 08-config-layer-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/config.js
  - src/MadeiraLineupPlanner.jsx
  - src/games/SharedLineupView.jsx
  - src/games/GameSummaryScreen.jsx
  - src/shared/utils.js
  - .env.local
  - .env.example
autonomous: false
requirements:
  - CFG-02

must_haves:
  truths:
    - "No code file under src/ contains the literal string 'Madeira' or 'MADEIRA' (grep returns zero matches) except for historical comments clearly marked as such"
    - "src/config.js exports TEAM_NAME sourced from import.meta.env.VITE_TEAM_NAME"
    - "Every former 'Madeira FC' user-facing string is rendered from TEAM_NAME (e.g., `${TEAM_NAME} FC` or `${TEAM_NAME}` alone where appropriate)"
    - "With VITE_TEAM_NAME=Madeira the share title, print header, screen header, shared lineup page title, and game summary score line render identically to pre-plan output"
  artifacts:
    - path: "src/config.js"
      provides: "Adds TEAM_NAME export and puts it on DEPLOYMENT.teamName"
      exports: ["TEAM_NAME"]
    - path: ".env.local"
      provides: "Sets VITE_TEAM_NAME=Madeira for the Madeira deployment"
      contains: "VITE_TEAM_NAME=Madeira"
    - path: ".env.example"
      provides: "Documents VITE_TEAM_NAME with placeholder"
      contains: "VITE_TEAM_NAME="
  key_links:
    - from: "src/MadeiraLineupPlanner.jsx"
      to: "src/config.js"
      via: "import { TEAM_NAME } from ./config"
      pattern: "import.*TEAM_NAME.*from.*config"
    - from: "src/games/SharedLineupView.jsx"
      to: "src/config.js"
      via: "import TEAM_NAME for MADEIRA FC title + logo alt text"
      pattern: "import.*TEAM_NAME"
    - from: "src/games/GameSummaryScreen.jsx"
      to: "src/config.js"
      via: "import TEAM_NAME for score line + share titles"
      pattern: "import.*TEAM_NAME"
    - from: "src/shared/utils.js"
      to: "src/config.js"
      via: "import TEAM_NAME for shareLineup title"
      pattern: "import.*TEAM_NAME"
---

<objective>
Replace every hardcoded "Madeira FC" / "MADEIRA FC" / "Madeira" string in the codebase with a value read from per-deployment config. Madeira's instance continues to render exactly as before because its config sets team name to "Madeira".

Purpose: Satisfies CFG-02. With team name in config, the only remaining Madeira-specific content is the roster and formation set (CFG-03 / CFG-04, handled in 08-03) plus null-safe num rendering (CFG-06, bundled into 08-03).
Output: Updated `src/config.js`, four source files that displayed "Madeira" now display `TEAM_NAME`, and updated `.env.local` / `.env.example`.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-config-layer-extraction/08-01-SUMMARY.md

@src/config.js
@src/MadeiraLineupPlanner.jsx
@src/games/SharedLineupView.jsx
@src/games/GameSummaryScreen.jsx
@src/shared/utils.js

<interfaces>
<!-- From 08-01: src/config.js already exports FIREBASE_CONFIG, GAME_STRUCTURE, DEPLOYMENT. -->
<!-- This plan adds TEAM_NAME to the same module. -->

Current "Madeira" literal occurrences (verified by grep):
- src/MadeiraLineupPlanner.jsx:847  `<title>Madeira FC Lineup</title>` (print window)
- src/MadeiraLineupPlanner.jsx:893  `<img ... alt="Madeira FC" ...>` (print logo alt)
- src/MadeiraLineupPlanner.jsx:895  `MADEIRA FC` (print header text)
- src/MadeiraLineupPlanner.jsx:921  `Madeira FC Lineup Planner` (print footer)
- src/MadeiraLineupPlanner.jsx:946  `<img ... alt="Madeira FC" ...>` (screen logo alt)
- src/MadeiraLineupPlanner.jsx:948  `MADEIRA FC` (screen header text)
- src/games/SharedLineupView.jsx:61  `<img ... alt="Madeira FC" ...>`
- src/games/SharedLineupView.jsx:62  `MADEIRA FC` (shared view title)
- src/games/GameSummaryScreen.jsx:62  `title: \`Madeira FC vs ${game.opponent}\``
- src/games/GameSummaryScreen.jsx:86  `title: "Madeira FC Game Summary"`
- src/games/GameSummaryScreen.jsx:255 `Madeira FC {homeScore} – {awayScore} {game.opponent}`
- src/shared/utils.js:109  `const title = data.name ? \`Madeira FC — ${data.name}\` : "Madeira FC Lineup";`

Also in comments (leave alone — comments aren't user-facing):
- src/MadeiraLineupPlanner.jsx:10  `// MADEIRA FC — 9v9 LINEUP PLANNER` (comment)
- src/MadeiraLineupPlanner.jsx:546,550  `madeira_${key}` (localStorage prefix — KEEP; migrating localStorage keys is out of scope)
- src/games/LiveGameScreen.jsx:18, 22, 31, 51  `madeira_${key}` (localStorage prefix — KEEP)
- src/shared/constants.js:2  `// SHARED CONSTANTS — Madeira Lineup Planner v2.0` (comment — leave alone)

NOT in scope this plan (intentional):
- `public/manifest.json "name": "Madeira FC Game Hub"` — PWA manifest is static JSON; build-time interpolation belongs to Phase 11.
- `index.html <title>Madeira FC Game Hub</title>` — Vite supports HTML env interpolation via `%VITE_TEAM_NAME%`. Worth doing if trivial; if it breaks dev reload, defer to 08-04 smoke-check and note in summary.
- `public/madeira-fc-logo.png` filename — leaving as-is (friend deployment ships own logo later; Phase 11 decides naming).
- `madeira_*` localStorage key prefixes — changing them wipes Madeira users' saved state. Out of scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TEAM_NAME to config + env files</name>
  <files>src/config.js, .env.local, .env.example, src/tests/config.test.js</files>
  <action>
    1. Read the current `src/config.js` (from 08-01).
    2. Add a new export:
       ```javascript
       export const TEAM_NAME = import.meta.env.VITE_TEAM_NAME || "";
       ```
       If empty at build time, throw an explicit error (`Error("VITE_TEAM_NAME is required — set it in .env.local or .env.example");`) — a missing team name is a deployment misconfiguration, not a sensible default.
    3. Update the `DEPLOYMENT` default export to include `teamName: TEAM_NAME` (replace the existing `teamName: undefined` placeholder reserved by 08-01).
    4. Update the top-of-file env var comment block to document `VITE_TEAM_NAME`.
    5. Update `.env.local` (root): add `VITE_TEAM_NAME=Madeira`.
    6. Update `.env.example` (root): add `VITE_TEAM_NAME=Your Team Name`.
    7. Update `src/tests/config.test.js` (from 08-01): add a test asserting TEAM_NAME reflects VITE_TEAM_NAME when set, and throws when unset/empty.
    8. Run `npx vitest run src/tests/config.test.js` — all tests must pass.
  </action>
  <verify>
    <automated>npx vitest run src/tests/config.test.js</automated>
  </verify>
  <done>
    - src/config.js exports TEAM_NAME
    - VITE_TEAM_NAME=Madeira in .env.local
    - Missing VITE_TEAM_NAME throws a clear error at config load time
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace "Madeira" string literals in src/ with TEAM_NAME</name>
  <files>
    src/MadeiraLineupPlanner.jsx,
    src/games/SharedLineupView.jsx,
    src/games/GameSummaryScreen.jsx,
    src/shared/utils.js
  </files>
  <action>
    Add `import { TEAM_NAME } from "./config";` (or `"../config"` from games/ and shared/) at the top of each file below, then replace every user-facing literal per the table:

    **src/MadeiraLineupPlanner.jsx** (import path `"./config"`):
    - Line ~847 (print window title): `title>Madeira FC Lineup` → `title>${TEAM_NAME} FC Lineup` (inside the template string).
    - Line ~893 (print logo alt): `alt="Madeira FC"` → `alt={\`${TEAM_NAME} FC\`}`.
    - Line ~895 (print header text): literal `MADEIRA FC` → `{TEAM_NAME.toUpperCase()} FC`.
    - Line ~921 (print footer): literal `Madeira FC Lineup Planner` → `{TEAM_NAME} FC Lineup Planner`.
    - Line ~946 (screen logo alt): `alt="Madeira FC"` → `alt={\`${TEAM_NAME} FC\`}`.
    - Line ~948 (screen header text): `MADEIRA FC` → `{TEAM_NAME.toUpperCase()} FC`.
    - NOTE: The localStorage prefix `madeira_` on lines 546/550 stays unchanged. The comment on line 10 can stay.

    **src/games/SharedLineupView.jsx** (import path `"../config"`):
    - Line ~61: `alt="Madeira FC"` → `alt={\`${TEAM_NAME} FC\`}`.
    - Line ~62: literal `MADEIRA FC` → `{TEAM_NAME.toUpperCase()} FC`.

    **src/games/GameSummaryScreen.jsx** (import path `"../config"`):
    - Line ~62: `title: \`Madeira FC vs ${game.opponent}\`` → `title: \`${TEAM_NAME} FC vs ${game.opponent}\``.
    - Line ~86: `title: "Madeira FC Game Summary"` → `title: \`${TEAM_NAME} FC Game Summary\``.
    - Line ~255 (score line JSX): `Madeira FC {homeScore} – {awayScore} {game.opponent}` → `{TEAM_NAME} FC {homeScore} – {awayScore} {game.opponent}`.
    - Line ~94 download filename: `madeira-vs-...` → use `TEAM_NAME.toLowerCase().replace(/\s+/g, "-")` to stay URL-safe.

    **src/shared/utils.js** (import path `"../config"`):
    - Line ~109: rewrite to use TEAM_NAME:
      ```javascript
      const title = data.name ? `${TEAM_NAME} FC — ${data.name}` : `${TEAM_NAME} FC Lineup`;
      ```

    After edits, verify with grep:
    ```bash
    grep -rn "Madeira\|MADEIRA" src/ | grep -v "^src/tests/" | grep -v "// " | grep -v "madeira_"
    ```
    Should return zero hits for UI-string literals. Acceptable remaining: comment lines (`//`), localStorage prefix `madeira_`, test files.

    Then run `npm run build`.

    AVOID:
    - Do NOT change localStorage key prefix `madeira_`.
    - Do NOT touch `public/manifest.json`, `index.html`, or `public/madeira-fc-logo.png` filename.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; ! (grep -rn "Madeira FC\|MADEIRA FC" src/ | grep -v "^src/tests/" | grep -v "^\s*//")</automated>
  </verify>
  <done>
    - Build succeeds
    - Grep for "Madeira FC" / "MADEIRA FC" in src/ returns no matches outside comments and tests
    - Each modified file imports TEAM_NAME from the correct relative path
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually walk the Madeira flow — every "Madeira FC" surface should render identically</name>
  <files>(no files modified — human runs dev server and inspects every user-facing "Madeira FC" surface)</files>
  <action>
    **Human verification step.** Claude should start `npm run dev` in a background terminal and pause until the user confirms. No source files are modified in this task. If any regression is reported, fix it before advancing to Plan 08-03.

    Context for the user:
    TEAM_NAME is now config-driven. With VITE_TEAM_NAME=Madeira set in .env.local, every UI surface that previously showed "Madeira FC" should render exactly the same bytes.

    Surfaces to inspect (must look identical to pre-plan):
    1. Lineup tab → top-left header says "MADEIRA FC" with "Lineup Planner" subtitle (desktop and mobile).
    2. Click Print on the Lineup tab → print preview window title "Madeira FC Lineup", header "MADEIRA FC / Game Day Lineup Card", footer "Madeira FC Lineup Planner".
    3. Save a lineup with a name, click Share → navigator.share or clipboard title "Madeira FC — {name}". Open the shared URL in a fresh tab → shared view shows "MADEIRA FC" header.
    4. Games tab → open a completed game (or create a test) → summary header shows "Madeira FC {homeScore} – {awayScore} {opponent}".
    5. On the summary, Share Link → title "Madeira FC vs {opponent}". Share Image → title "Madeira FC Game Summary", filename `madeira-vs-{opponent}.png`.
    6. DevTools → Elements → Ctrl+F "Madeira" → all hits are rendered text, no hardcoded attribute values.
    7. DevTools → Console → zero errors referencing TEAM_NAME or config.

    Negative check:
    - Temporarily blank out `VITE_TEAM_NAME=` in .env.local and restart dev server → app refuses to boot with a clear error about missing VITE_TEAM_NAME. Restore to "Madeira".

    Expected: Pixel-identical rendering vs pre-plan. Every "Madeira" appearance sourced from config, not hardcoded.

    Resume signal: User types "approved" if every surface renders identically, or describes any cosmetic/functional difference. Claude must NOT proceed to Plan 08-03 until approval.
  </action>
  <verify>
    <automated>MISSING — human-verify checkpoint confirming UI parity (no automated visual regression harness in this project).</automated>
  </verify>
  <done>
    - User has walked all 7 surfaces in a running Madeira dev build
    - Every "Madeira FC" surface renders identically to pre-plan
    - User has typed "approved" (or the equivalent) to unblock Plan 08-03
  </done>
</task>

</tasks>

<verification>
- Automated: `npm run build` succeeds; `npx vitest run` passes; grep for "Madeira FC" / "MADEIRA FC" in src/ (excluding comments and tests) returns zero hits
- Manual (checkpoint): All 7 surfaces render identically to pre-plan
</verification>

<success_criteria>
- CFG-02: Team name loaded from VITE_TEAM_NAME via src/config.js; every user-facing "Madeira" string replaced by `${TEAM_NAME}` interpolation
- Madeira's existing .env.local sets VITE_TEAM_NAME=Madeira, so UI is byte-identical to pre-plan
- Missing VITE_TEAM_NAME throws a clear config-load error (fail-fast, not silent fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/08-config-layer-extraction/08-02-SUMMARY.md` per the summary template. Note any "Madeira" literals intentionally left (manifest.json, index.html, logo filename, localStorage prefix) so Phase 11 planning knows what's still pending.
</output>
