---
phase: 01-display-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MadeiraLineupPlanner.jsx
autonomous: false
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
must_haves:
  truths:
    - "Field positions show abbreviated names in 'J. Smith' format below the circle"
    - "Names already entered as initials (e.g., 'M.C.') are not re-abbreviated"
    - "Single-word names display as-is with no abbreviation"
    - "Bench/roster continues to show full player names"
    - "Position circles are ~56px (normal) and ~44px (compact) with proportionally larger text"
    - "Print view uses 'J. Smith' format but keeps existing circle sizes"
    - "Empty positions remain unchanged (dashed circle with position label)"
  artifacts:
    - path: "src/MadeiraLineupPlanner.jsx"
      provides: "abbreviateName utility, updated FieldPosition sizing, updated PrintPitch name formatting"
      contains: "abbreviateName"
  key_links:
    - from: "FieldPosition component"
      to: "abbreviateName function"
      via: "name display text content"
      pattern: "abbreviateName\\(player\\.name\\)"
    - from: "PrintPitch component"
      to: "abbreviateName function"
      via: "print name display text content"
      pattern: "abbreviateName\\(player\\.name\\)"
---

<objective>
Make player names immediately readable on the field and in print by abbreviating field names to "J. Smith" format, enlarging position circles with proportionally bigger text, and applying the same name formatting to print view.

Purpose: Coaches can identify players at a glance without zooming or squinting, whether on screen or on a printed sheet.
Output: Updated MadeiraLineupPlanner.jsx with name abbreviation logic, larger circles, and print name formatting.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-display-polish/01-CONTEXT.md

<interfaces>
<!-- Key code the executor needs. All in src/MadeiraLineupPlanner.jsx. -->

Current name extraction (line 170, FieldPosition):
```javascript
player.name.split(" ").slice(-1)[0]  // extracts last name only
```

Current name extraction (line 254, PrintPitch):
```javascript
player.name.split(" ").slice(-1)[0]  // same pattern
```

Current FieldPosition sizing constants (line 144-146):
```javascript
const circleSize = has ? (compact ? 36 : 46) : (compact ? 26 : 34);
const numSize = has ? (compact ? 13 : 17) : (compact ? 7 : 9);
const nameSize = has ? (compact ? 8 : 10) : (compact ? 7 : 9);
```

Current FieldPosition name label maxWidth (line 169):
```javascript
maxWidth: compact ? 56 : 76
```

Current FieldPosition position label font size (line 171):
```javascript
fontSize: compact ? 6 : 8
```

Current PrintPitch circle sizes (line 244):
```javascript
width: player ? 24 : 18, height: player ? 24 : 18
```

Current PrintPitch name font size (line 252):
```javascript
fontSize: 6
```

Bench (PlayerChip, line 198-199) already shows full name:
```javascript
{player.name}  // no change needed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add abbreviateName utility and update FieldPosition sizing and name format</name>
  <files>src/MadeiraLineupPlanner.jsx</files>
  <action>
1. Create an `abbreviateName(name)` utility function near the top of the file (after the color constants block, before FieldPosition). The function must:
   - Handle single-word names: return as-is (e.g., "Pele" returns "Pele")
   - Detect names where the first part is already initials (contains a period, e.g., "M.C. Smith"): return the full name as-is ("M.C. Smith")
   - Standard multi-word names: take the first character of the first word + "." + space + remaining words joined (e.g., "Mary Smith" returns "M. Smith", "Carlos De La Cruz" returns "C. De La Cruz")
   - Handle empty/null input: return empty string

2. In `FieldPosition` component (line 142), update sizing constants:
   - `circleSize`: `has ? (compact ? 44 : 56) : (compact ? 32 : 42)` (was 36/46 and 26/34)
   - `numSize`: `has ? (compact ? 15 : 20) : (compact ? 8 : 10)` (was 13/17 and 7/9)
   - `nameSize`: `has ? (compact ? 10 : 12) : (compact ? 8 : 10)` (was 8/10 and 7/9)
   - Name label `maxWidth`: `compact ? 68 : 90` (was 56/76 — wider to accommodate abbreviated names)
   - Position label fontSize: `compact ? 7 : 10` (was 6/8)

3. In `FieldPosition`, replace the name text content (line 170):
   - Change `player.name.split(" ").slice(-1)[0]` to `abbreviateName(player.name)`

4. In `PrintPitch` component (line 254), replace the name text content:
   - Change `player.name.split(" ").slice(-1)[0]` to `abbreviateName(player.name)`
   - Keep print circle sizes unchanged (24px filled, 18px empty) per user decision
   - Keep print name fontSize at 6 (no size change for print)

5. DISP-02 (bench full names) is already satisfied — do NOT change PlayerChip.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - abbreviateName function exists and handles: standard names ("Mary Smith" -> "M. Smith"), initial names ("M.C. Smith" -> "M.C. Smith"), single-word names ("Pele" -> "Pele"), empty input
    - FieldPosition uses abbreviateName for name display and has updated circle/text sizes (56px normal, 44px compact)
    - PrintPitch uses abbreviateName for name display with print circle sizes unchanged
    - PlayerChip (bench) unchanged — still shows full name
    - Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of display changes</name>
  <action>User verifies name abbreviation formatting and enlarged position circles on both screen and print views.</action>
  <verify>
    1. Run `npm run dev` and open the app in a browser
    2. Verify field positions show abbreviated names below circles (e.g., "M. Smith" not "Smith")
    3. Verify the bench/roster still shows full player names
    4. Verify position circles are visibly larger than before with bigger number and name text
    5. Verify empty positions still show dashed circles with position labels (unchanged behavior)
    6. If any player has an initial-style first name (e.g., "M.C."), confirm it shows as "M.C. Smith" not "M. Smith"
    7. Open browser print preview (Cmd+P) and verify field names show "J. Smith" format, circle sizes are small (unchanged from before), and bench shows full names
    8. Check that positions do not overlap in dense formations (e.g., 4-3-1)
  </verify>
  <done>User approves the visual display of abbreviated names, enlarged circles, and print formatting</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- Visual inspection confirms abbreviated names on field, full names on bench
- Circle sizes are larger on screen, unchanged in print
- No position overlap in dense formations
</verification>

<success_criteria>
- Field positions display "J. Smith" format names (DISP-01)
- Bench displays full player names (DISP-02 — unchanged, already works)
- Position circles are ~56px with proportionally larger text (DISP-03)
- Print view matches screen name format with existing print circle sizes
- Build passes, no visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-display-polish/01-01-SUMMARY.md`
</output>
