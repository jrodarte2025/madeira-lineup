---
phase: 03-mobile-ux-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MadeiraLineupPlanner.jsx
autonomous: false
requirements:
  - MOB-01
  - MOB-04

must_haves:
  truths:
    - "On mobile, a horizontal scrollable chip strip of available players appears above the field"
    - "On mobile, the header shows only the logo and Lineup Planner title — no controls"
    - "On mobile, the bench bar at the bottom is completely hidden"
    - "On mobile, the roster drawer is completely removed"
    - "Chips show jersey number + first name (e.g. '7 Alex') and are tappable"
    - "When all players are assigned, strip shows 'All players assigned' placeholder maintaining height"
    - "Desktop layout is unchanged by these modifications"
  artifacts:
    - path: "src/MadeiraLineupPlanner.jsx"
      provides: "Mobile chip strip above field, minimal header, hidden bench bar and drawer on mobile"
      contains: "chip strip"
  key_links:
    - from: "chip strip"
      to: "availablePlayers state"
      via: "maps over availablePlayers array"
      pattern: "availablePlayers\\.map"
    - from: "chip onClick"
      to: "handlePlayerClick"
      via: "existing click handler"
      pattern: "handlePlayerClick"
---

<objective>
Restructure the mobile layout top section: create a horizontal scrollable player chip strip above the pitch, minimize the header to logo+title only, and hide the bench bar and roster drawer on mobile. Desktop remains untouched.

Purpose: Coaches on phones see available players immediately above the field without opening drawers — the core mobile UX improvement.
Output: Mobile layout with chip strip above field, minimal header, no bench bar or drawer on mobile.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-ux-overhaul/03-CONTEXT.md

<interfaces>
<!-- Key patterns from MadeiraLineupPlanner.jsx the executor needs -->

Responsive hook (line 72):
```javascript
const isMobile = useMediaQuery("(max-width: 767px)");
const isTablet = useMediaQuery("(min-width: 768px) and (max-width: 1024px)");
```

Available players array (used by bench bar, line ~1038):
```javascript
availablePlayers  // Array of { id, name, num } — players not in currentLineup and not inactive
```

Existing bench chip pattern (line ~1041-1054):
```javascript
availablePlayers.map((p) => (
  <div key={p.id} draggable={!isMobile}
    onDragStart={!isMobile ? (e) => handleDragStart(e, p.id, "roster") : undefined}
    onDragEnd={handleDragEnd}
    onClick={() => handlePlayerClick(p.id)}
    style={{ /* chip styles */ }}>
    <span>{p.num}</span>
    <span>{p.name.split(" ")[0]}</span>
  </div>
))
```

Selected player state:
```javascript
const [selectedPlayer, setSelectedPlayer] = useState(null);
// selectedPlayer === p.id → orange highlight on chip
```

Color constants: `C.orange`, `C.white`, `C.whiteAlpha`, `C.navyDark`
Fonts: `fontDisplay`, `fontBase`

Layout structure (line ~880-1097):
- Screen view div > Header > Main (sidebar + pitch) > Bench bar > Mobile drawer
- Main uses `flexDirection: isMobile ? "column" : "row"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile minimal header and chip strip above field</name>
  <files>src/MadeiraLineupPlanner.jsx</files>
  <action>
  Make these changes to the mobile layout in MadeiraLineupPlanner.jsx. All changes use the existing `isMobile` flag — desktop code paths must not be modified.

  **1. Minimize the mobile header (line ~884-971):**
  On mobile, the header should show ONLY the logo and "MADEIRA FC / Lineup Planner" title. Wrap the formation selector div (line ~900-910), the half toggle + controls div (line ~912-970) in `{!isMobile && (...)}` blocks so they render only on desktop. The header container on mobile should not use `flexWrap: "wrap"` — just a simple row with logo+title.

  **2. Create chip strip above the pitch (insert between header and main content):**
  Add a new `{isMobile && (...)}` block between the header and the main content div (before line ~974). This chip strip:
  - Container: horizontal scrollable row, `overflowX: "auto"`, `WebkitOverflowScrolling: "touch"`, padding `10px 12px`, background `rgba(0,0,0,0.15)`, borderBottom `1px solid ${C.whiteAlpha}`, `display: "flex"`, `gap: 8`, `alignItems: "center"`, min-height ~48px (so layout doesn't shift when empty)
  - If `availablePlayers.length === 0`: show `<div>` with text "All players assigned" in italic, color `rgba(255,255,255,0.25)`, fontSize 12
  - If `selectedPlayer` is set but `availablePlayers.length > 0`: show a subtle hint "Tap a position to assign" after the chips (as the last element, flexShrink 0, italic, fontSize 10, color rgba(255,255,255,0.3))
  - Each chip: `onClick={() => handlePlayerClick(p.id)}`, NO draggable (mobile is tap-only). Style each chip as a pill: `padding: "8px 14px"`, `borderRadius: 20`, `fontSize: 13`, `fontWeight: 600`, `fontFamily: fontBase`, `background: selectedPlayer === p.id ? C.orange : "rgba(255,255,255,0.08)"`, `border: 1px solid ${selectedPlayer === p.id ? C.orange : "rgba(255,255,255,0.12)"}`, `color: C.white`, `whiteSpace: "nowrap"`, `flexShrink: 0`, `cursor: "pointer"`, `userSelect: "none"`, `transition: "all 0.15s ease"`, min-height 44px (touch target), display flex, alignItems center, gap 6.
  - Inside each chip: `<span style={{ fontFamily: fontDisplay, fontWeight: 800, fontSize: 12 }}>{p.num}</span>` then `<span>{p.name.split(" ")[0]}</span>`

  **3. Hide bench bar on mobile (line ~1016-1065):**
  Wrap the entire bench bar div in a condition so it only renders on desktop. Change the bench bar to: `{!isMobile && (<div onDragOver={handleRosterDragOver} ...>...</div>)}`. This completely removes the bench bar on mobile.

  **4. Remove mobile roster drawer (line ~1068-1097):**
  Delete the entire `{isMobile && (<>...backdrop...drawer...</>)}` block that renders the mobile roster drawer. The chip strip replaces its player-selection function. The roster management function (add/edit/remove players) will be handled in Plan 02 via a button in the controls section below the field.

  **5. Keep `rosterOpen` state** — do not remove it. Plan 02 will repurpose it for the roster management modal accessible from the controls below the field.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>On mobile viewport: header shows only logo+title, chip strip with available players appears above the pitch, bench bar is hidden, roster drawer is removed. Desktop layout renders exactly as before with full header controls and bench bar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify mobile chip strip and minimal header</name>
  <action>
  Human verifies the mobile layout restructure visually in browser.
  </action>
  <what-built>Mobile layout restructure: minimal header, chip strip above field replacing bench bar and drawer</what-built>
  <how-to-verify>
    1. Run `npx vite dev` and open in browser
    2. Open DevTools, toggle to mobile viewport (iPhone 14 or similar, ~390px wide)
    3. Verify: Header shows only logo + "MADEIRA FC / Lineup Planner" — no formation buttons, no half toggle, no save/load/print/share buttons
    4. Verify: Below header, a horizontal scrollable chip strip shows available players as pills (e.g., "2 Alex", "6 Avery")
    5. Tap a player chip — it highlights orange. Tap a field position — player assigns. Chip disappears from strip.
    6. Assign all players — strip shows "All players assigned" text, strip area maintains height
    7. Switch to desktop viewport (1200px+) — verify full header with all controls, sidebar roster, bench bar all render exactly as before
  </how-to-verify>
  <verify>Human visual verification in browser</verify>
  <done>Mobile chip strip and minimal header approved by user</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with no errors
- Mobile viewport (390px): only logo+title in header, chip strip visible above pitch, no bench bar, no drawer
- Desktop viewport (1200px+): full header, sidebar, bench bar all unchanged
- Tap interaction works: tap chip to select, tap position to assign
</verification>

<success_criteria>
- Mobile header is minimal (logo + title only)
- Chip strip renders available players above the field on mobile
- Bench bar hidden on mobile
- Roster drawer removed on mobile
- Desktop layout pixel-for-pixel identical to pre-phase state
- All tap interactions (select player, assign to position) work on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-ux-overhaul/03-01-SUMMARY.md`
</output>
