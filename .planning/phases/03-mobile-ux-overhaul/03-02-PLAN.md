---
phase: 03-mobile-ux-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/MadeiraLineupPlanner.jsx
autonomous: false
requirements:
  - MOB-02
  - MOB-03
  - PRES-01

must_haves:
  truths:
    - "On mobile, formation selector buttons appear below the field"
    - "On mobile, half toggle (1ST/2ND) appears below the field"
    - "On mobile, action buttons (Save, Load, Print, Share, Roster) appear below the field with icons and short labels"
    - "On mobile, all interactive elements (chips, buttons, formation selectors) have minimum 44px tap targets"
    - "On mobile, Roster button opens roster management (add/edit/remove players, mark inactive)"
    - "On mobile, field position circles are 50px (no compact mode shrinkage)"
    - "Desktop layout is pixel-for-pixel identical to before Phase 3"
  artifacts:
    - path: "src/MadeiraLineupPlanner.jsx"
      provides: "Controls below field on mobile, touch target sizing, roster management access"
      contains: "formation selector"
  key_links:
    - from: "formation buttons below field"
      to: "setFormation state"
      via: "onClick handler"
      pattern: "setFormation"
    - from: "Roster button"
      to: "rosterOpen state"
      via: "setRosterOpen(true)"
      pattern: "setRosterOpen"
    - from: "FieldPosition compact prop"
      to: "mobile circle sizing"
      via: "compact={false} on mobile"
      pattern: "compact="
---

<objective>
Add formation selector, half toggle, and action buttons below the field on mobile. Size all touch targets to 44px minimum. Wire Roster button to roster management modal. Remove compact mode on mobile field positions. Verify desktop is unchanged.

Purpose: Complete the mobile top-to-bottom flow (header > chips > pitch > controls) and ensure all interactive elements are thumb-friendly.
Output: Fully functional mobile layout with controls below field, proper touch targets, and roster management access.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-ux-overhaul/03-CONTEXT.md
@.planning/phases/03-mobile-ux-overhaul/03-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, the mobile layout is: minimal header > chip strip > pitch area (no bench bar, no drawer) -->
<!-- Plan 02 adds controls BELOW the pitch area on mobile -->

State and handlers needed:
```javascript
const [formation, setFormation] = useState("3-3-2");
const [activeHalf, setActiveHalf] = useState(1);
const [rosterOpen, setRosterOpen] = useState(false); // Repurpose for roster management modal
const [modalOpen, setModalOpen] = useState(false);
const [modalMode, setModalMode] = useState("save");
```

Formation keys: `Object.keys(FORMATIONS)` — e.g., "3-3-2", "4-3-1", etc.

RosterContent component (line ~396):
```javascript
function RosterContent({ roster, availablePlayers, onFieldPlayers, inactivePlayers, selectedPlayer, showEdit, setShowEdit,
  onAddPlayer, onRemovePlayer, onEditPlayer, onToggleInactive, onDragStart, onDragEnd, onPlayerClick })
```

rosterProps object already defined in component — spreads all needed props to RosterContent.

FieldPosition compact prop (line ~1003):
```javascript
compact={isMobile}  // Currently shrinks circles to 40px on mobile — change to compact={false} or remove
```

Existing SVG icons: Save, Load, Print, Share icons already exist as inline SVGs in the header controls section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controls below field and touch target sizing</name>
  <files>src/MadeiraLineupPlanner.jsx</files>
  <action>
  All changes use the existing `isMobile` flag. Desktop code paths must not be modified.

  **1. Add controls section below the pitch on mobile:**
  After the pitch container div (the one with `aspectRatio: "3 / 4"`) but still inside the pitch's parent flex column, add a `{isMobile && (...)}` block with these stacked rows:

  Container: `width: "100%"`, `maxWidth: 360`, `display: "flex"`, `flexDirection: "column"`, `gap: 10`, `padding: "12px 0"`.

  **Row 1 — Formation selector:**
  Horizontal row of formation buttons. Container: `display: "flex"`, `gap: 3`, `background: "rgba(0,0,0,0.3)"`, `borderRadius: 10`, `padding: 3`. Each button: `flex: 1`, `padding: "10px 0"` (ensures 44px+ height), `borderRadius: 7`, `border: "none"`, `cursor: "pointer"`, `fontFamily: fontDisplay`, `fontWeight: 700`, `fontSize: 13`, background/color follows same active/inactive pattern as desktop (active: `C.orange` bg + `C.white` text, inactive: transparent + `rgba(255,255,255,0.45)`). `onClick={() => setFormation(f)}`.

  **Row 2 — Half toggle:**
  Two buttons for 1ST / 2ND. Container: `display: "flex"`, `background: "rgba(0,0,0,0.3)"`, `borderRadius: 10`, `padding: 3`. Each button: `flex: 1`, `padding: "10px 0"`, `borderRadius: 7`, `border: "none"`, `cursor: "pointer"`, `fontFamily: fontDisplay`, `fontWeight: 700`, `fontSize: 13`. Active: `C.orange` bg, inactive: transparent. `onClick={() => { setActiveHalf(h); setSelectedPlayer(null); }}`.

  **Row 3 — Action buttons:**
  Horizontal row with Save, Load, Print, Share, Roster buttons. Container: `display: "flex"`, `gap: 6`, `flexWrap: "wrap"`, `justifyContent: "center"`. Each button: `flex: "1 1 auto"`, `minWidth: 60`, `padding: "10px 8px"` (44px+ height), `borderRadius: 8`, `border: "1px solid rgba(255,255,255,0.18)"`, `background: "transparent"`, `color: "rgba(255,255,255,0.6)"`, `cursor: "pointer"`, `fontFamily: fontBase`, `fontSize: 11`, `fontWeight: 600`, `display: "flex"`, `flexDirection: "column"`, `alignItems: "center"`, `gap: 4`.

  Each action button has an SVG icon (reuse the exact same SVG icons from the header section) + a short label below:
  - Save: floppy icon + "Save" — `onClick={() => { setModalMode("save"); setModalOpen(true); }}`
  - Load: folder icon + "Load" — `onClick={() => { setModalMode("load"); setModalOpen(true); }}` — include badge for `savedLineups.length > 0`
  - Print: printer icon + "Print" — `onClick={handlePrint}`
  - Share: share icon + "Share" — `onClick={handleShareCurrent}` — use `border: 1px solid ${C.orange}`, `background: "rgba(232,100,32,0.1)"`, `color: C.orange`
  - Roster: people icon (reuse from old roster toggle button SVG) + "Roster" — `onClick={() => setRosterOpen(true)}` — use `border: 1px solid ${C.orange}`, `background: "rgba(232,100,32,0.12)"`, `color: C.orange`

  **2. Roster management modal on mobile:**
  Repurpose the `rosterOpen` state. When `rosterOpen` is true on mobile, render a full-screen modal (not a bottom drawer):
  - Backdrop: `position: "fixed"`, `inset: 0`, `background: "rgba(0,0,0,0.6)"`, `zIndex: 50`, onClick closes
  - Modal: `position: "fixed"`, `top: "5vh"`, `left: "4vw"`, `right: "4vw"`, `bottom: "5vh"`, `background: C.navyDark`, `borderRadius: 14`, `zIndex: 51`, `display: "flex"`, `flexDirection: "column"`, `overflow: "hidden"`
  - Modal header: "Roster Management" title + close X button, padding 14px, borderBottom
  - Modal body: `flex: 1`, `overflow: "auto"`, render `<RosterContent {...rosterProps} />` inside

  This replaces the old bottom drawer that was removed in Plan 01.

  **3. Remove compact mode on mobile field positions:**
  Change `compact={isMobile}` to `compact={false}` (or simply remove the compact prop) on the FieldPosition component (around line ~1003). This ensures mobile field circles use the full 50px size. The FieldPosition component can keep accepting the `compact` prop — just stop passing `true` on mobile.

  **4. Ensure chip strip touch targets from Plan 01 are at least 44px:**
  If Plan 01's chip strip chips don't already have `minHeight: 44` in their styles, add it. This is a safety check — Plan 01 should have set this but verify.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>On mobile: formation selector, half toggle, and action buttons (Save, Load, Print, Share, Roster) appear below the field in stacked rows. All buttons are 44px+ tap targets. Roster button opens full-screen roster management modal. Field circles are full 50px on mobile. Desktop layout unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete mobile UX overhaul and desktop preservation</name>
  <action>
  Human verifies the complete mobile UX overhaul and confirms desktop is unchanged.
  </action>
  <what-built>Complete mobile UX overhaul: chip strip above field, controls below field, touch targets, roster management modal, full 50px field circles</what-built>
  <how-to-verify>
    1. Run `npx vite dev` and open in browser
    2. Open DevTools, toggle to mobile viewport (iPhone 14, ~390px wide)
    3. Verify complete top-to-bottom flow: minimal header > chip strip > pitch > formation buttons > half toggle > action buttons
    4. Tap formation buttons below field — formation changes, pitch updates
    5. Tap 1ST/2ND half toggle — half switches
    6. Tap Save/Load/Print/Share buttons — each triggers correct action (modal opens, print dialog, share)
    7. Tap Roster button — full-screen roster management modal opens with player list, edit controls
    8. Close roster modal — returns to main view
    9. Verify field circles on mobile are 50px (not shrunk/compact)
    10. Verify all buttons and chips feel comfortably tappable (44px+ targets)
    11. Switch to desktop viewport (1200px+):
        - Full header with formation selector, half toggle, save/load/print/share buttons
        - Sidebar roster on left
        - Bench bar at bottom
        - No controls section below the field
        - Everything looks exactly as it did before Phase 3
  </how-to-verify>
  <verify>Human visual verification in browser</verify>
  <done>Complete mobile UX overhaul approved. Desktop preservation confirmed.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with no errors
- Mobile (390px): formation/half/action controls below field, 44px+ touch targets, roster modal works
- Mobile: field circles 50px, not compact
- Desktop (1200px+): identical to pre-Phase 3 — header controls, sidebar, bench bar all present and unchanged
- All interactions work on mobile: select chip > tap position, change formation, switch half, save/load/print/share, roster management
</verification>

<success_criteria>
- Formation selector renders below field on mobile with full-width buttons
- Half toggle renders below field on mobile
- Action buttons (Save, Load, Print, Share, Roster) render below field on mobile with icons and labels
- All mobile interactive elements have 44px+ tap targets
- Roster button opens full-screen modal with RosterContent
- Field position circles are 50px on mobile (no compact shrinkage)
- Desktop layout is pixel-for-pixel identical to pre-Phase 3 state
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-ux-overhaul/03-02-SUMMARY.md`
</output>
