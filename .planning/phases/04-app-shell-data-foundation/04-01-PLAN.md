---
phase: 04-app-shell-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/constants.js
  - src/shared/PitchSVG.jsx
  - src/shared/FieldPosition.jsx
  - src/shared/utils.js
  - src/MadeiraLineupPlanner.jsx
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "C, FORMATIONS, STAT_TYPES, POSITION_GROUP, STAT_COLORS are importable from shared/constants.js"
    - "PitchSVG renders identically when imported from shared/PitchSVG.jsx"
    - "FieldPosition renders identically when imported from shared/FieldPosition.jsx"
    - "abbreviateName, useMediaQuery, encodeLineup, decodeLineup are importable from shared/utils.js"
    - "MadeiraLineupPlanner still works after all extractions — no visual or functional regressions"
  artifacts:
    - path: "src/shared/constants.js"
      provides: "C colors, FORMATIONS, STAT_TYPES, POSITION_GROUP, STAT_COLORS"
      exports: ["C", "FORMATIONS", "STAT_TYPES", "POSITION_GROUP", "STAT_COLORS"]
    - path: "src/shared/PitchSVG.jsx"
      provides: "Soccer pitch SVG component"
      exports: ["default"]
    - path: "src/shared/FieldPosition.jsx"
      provides: "Interactive player circle component"
      exports: ["default"]
    - path: "src/shared/utils.js"
      provides: "Utility functions"
      exports: ["abbreviateName", "useMediaQuery", "encodeLineup", "decodeLineup"]
  key_links:
    - from: "src/MadeiraLineupPlanner.jsx"
      to: "src/shared/constants.js"
      via: "import { C, FORMATIONS } from './shared/constants'"
      pattern: "import.*from.*shared/constants"
    - from: "src/MadeiraLineupPlanner.jsx"
      to: "src/shared/PitchSVG.jsx"
      via: "import PitchSVG from './shared/PitchSVG'"
      pattern: "import.*PitchSVG.*from.*shared"
    - from: "src/MadeiraLineupPlanner.jsx"
      to: "src/shared/FieldPosition.jsx"
      via: "import FieldPosition from './shared/FieldPosition'"
      pattern: "import.*FieldPosition.*from.*shared"
    - from: "src/shared/FieldPosition.jsx"
      to: "src/shared/constants.js"
      via: "import { C } from './constants'"
      pattern: "import.*C.*from.*constants"
---

<objective>
Extract shared components and constants from the MadeiraLineupPlanner monolith into reusable `src/shared/` modules.

Purpose: PitchSVG, FieldPosition, C colors, and utility functions will be consumed by the game screen (Phase 5) and stats views (Phase 7). Extracting now prevents duplication and establishes the shared module pattern for all v2.0 features.

Output: Four new files in `src/shared/`, MadeiraLineupPlanner.jsx updated to import from them.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-app-shell-data-foundation/04-CONTEXT.md
@.planning/phases/04-app-shell-data-foundation/04-RESEARCH.md

@src/MadeiraLineupPlanner.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared constants and utilities</name>
  <files>src/shared/constants.js, src/shared/utils.js</files>
  <action>
Create `src/shared/` directory and two files:

**src/shared/constants.js** — Move these from MadeiraLineupPlanner.jsx:
- `C` color object (lines 45-54). Add three new stat color properties: `statOffensive: "#E86420"`, `statDefensive: "#4CAFB6"`, `statNeutral: "#6b7280"`.
- `fontBase` and `fontDisplay` string constants (lines 56-57).
- `FORMATIONS` object (lines 100-121).
- `INITIAL_ROSTER` array (lines 84-98).
- Add NEW constants (not in the monolith — defined per CONTEXT.md decisions):
  - `POSITION_GROUP = { GK: "GK", DEF: "DEF", MID: "MID", FWD: "FWD" }`
  - `STAT_TYPES` — placeholder stat type strings grouped by category (offensive: goal, assist, shot; defensive: tackle, clearance, save; neutral: foul). Exact stat names may be refined in Phase 5 but the structure and categories must be correct now.
  - `STAT_COLORS` — map each stat type string to its color from `C.statOffensive`, `C.statDefensive`, or `C.statNeutral`.
- Export all as named exports.

**src/shared/utils.js** — Move these from MadeiraLineupPlanner.jsx:
- `abbreviateName` function (lines 62-68).
- `useMediaQuery` hook (lines 73-82). It imports from React so add `import { useState, useEffect } from "react"`.
- `encodeLineup` function (lines 12-15).
- `decodeLineup` function (lines 17-22).
- `buildShareUrl` function (lines 24-27) — keep as-is for now; Plan 04-02 will update it for HashRouter.
- `shareLineup` function (lines 29-43).
- Export all as named exports.

Do NOT remove the originals from MadeiraLineupPlanner.jsx yet — that happens in Task 2.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && node -e "const c = require('./src/shared/constants.js'); console.log(Object.keys(c))" 2>&1 || node -e "import('./src/shared/constants.js').then(m => console.log(Object.keys(m)))" 2>&1; npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>shared/constants.js exports C (with stat colors), fontBase, fontDisplay, FORMATIONS, INITIAL_ROSTER, POSITION_GROUP, STAT_TYPES, STAT_COLORS. shared/utils.js exports abbreviateName, useMediaQuery, encodeLineup, decodeLineup, buildShareUrl, shareLineup. Both files exist and have no syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extract PitchSVG and FieldPosition, rewire monolith</name>
  <files>src/shared/PitchSVG.jsx, src/shared/FieldPosition.jsx, src/MadeiraLineupPlanner.jsx</files>
  <action>
**src/shared/PitchSVG.jsx** — Extract the `PitchSVG` component (lines 126-148 of MadeiraLineupPlanner.jsx). It is a pure component with no dependencies on `C` or other imports — just takes a `lineColor` prop. Default export.

**src/shared/FieldPosition.jsx** — Extract the `FieldPosition` component (lines 154-204 of MadeiraLineupPlanner.jsx). This component uses `C`, `fontBase`, `fontDisplay`, and `abbreviateName`. Add these imports at the top:
```jsx
import { C, fontBase, fontDisplay } from "./constants";
import { abbreviateName } from "./utils";
```
Default export.

**src/MadeiraLineupPlanner.jsx** — Update the monolith:
1. Add imports at the top:
   ```jsx
   import { C, fontBase, fontDisplay, FORMATIONS, INITIAL_ROSTER } from "./shared/constants";
   import { abbreviateName, useMediaQuery, encodeLineup, decodeLineup, buildShareUrl, shareLineup } from "./shared/utils";
   import PitchSVG from "./shared/PitchSVG";
   import FieldPosition from "./shared/FieldPosition";
   ```
2. DELETE the original definitions of ALL extracted items from MadeiraLineupPlanner.jsx:
   - `C` object, `fontBase`, `fontDisplay` constants
   - `FORMATIONS` object, `INITIAL_ROSTER` array
   - `abbreviateName`, `useMediaQuery`, `encodeLineup`, `decodeLineup`, `buildShareUrl`, `shareLineup` functions
   - `PitchSVG` component, `FieldPosition` component
3. Keep everything else in MadeiraLineupPlanner.jsx exactly as-is (all state, handlers, render logic).
4. Verify NO duplicate definitions of `C` remain in MadeiraLineupPlanner.jsx (Pitfall 3 from RESEARCH.md).

After this task, run `npm run dev` and verify the lineup builder renders correctly with all interactions working (drag-drop, touch, click).
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>MadeiraLineupPlanner.jsx imports all shared items from src/shared/. No duplicate definitions exist. `npx vite build` succeeds with zero errors. The lineup builder renders and functions identically to before extraction — field circles, drag-and-drop, touch interactions, bench, formations all work.</done>
</task>

</tasks>

<verification>
- `npx vite build` completes with no errors
- `grep -r "const C = " src/` returns exactly ONE match (in shared/constants.js)
- `grep -r "function PitchSVG" src/` returns exactly ONE match (in shared/PitchSVG.jsx)
- `grep -r "function FieldPosition" src/` returns exactly ONE match (in shared/FieldPosition.jsx)
- All shared files have proper imports and exports
</verification>

<success_criteria>
- Six shared constants/types exported from constants.js (C, FORMATIONS, INITIAL_ROSTER, STAT_TYPES, POSITION_GROUP, STAT_COLORS)
- PitchSVG and FieldPosition are standalone components in shared/
- MadeiraLineupPlanner.jsx has no duplicate definitions — all come from imports
- Build passes, app functions identically to v1.0
</success_criteria>

<output>
After completion, create `.planning/phases/04-app-shell-data-foundation/04-01-SUMMARY.md`
</output>
