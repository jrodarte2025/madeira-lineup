---
phase: 05-live-game
plan: 01
type: execute
wave: 2
depends_on: ["05-00"]
files_modified:
  - src/shared/constants.js
  - src/firebase.js
  - src/tabs/GamesTab.jsx
  - src/App.jsx
autonomous: true
requirements: [GAME-01, GAME-02]

must_haves:
  truths:
    - "Coach can tap + New Game on the Games tab to open a creation modal"
    - "Coach fills in opponent name and date (defaults to today), taps Create, and the game is saved to Firestore"
    - "Games tab shows a list of past/in-progress games as cards with opponent, date, status, and score"
    - "After creating a game, the app navigates to /games/:id (LiveGameScreen placeholder)"
    - "POSITION_STATS and expanded STAT_COLORS are importable from constants.js"
  artifacts:
    - path: "src/shared/constants.js"
      provides: "POSITION_STATS map and expanded STAT_COLORS for all stat types"
      contains: "POSITION_STATS"
    - path: "src/firebase.js"
      provides: "listGames() and replaceGameEvents() functions"
      exports: ["listGames", "replaceGameEvents"]
    - path: "src/tabs/GamesTab.jsx"
      provides: "Game list with cards + GameSetupModal + New Game button"
      min_lines: 80
    - path: "src/App.jsx"
      provides: "/games/:id route for live game screen"
      contains: "games/:id"
  key_links:
    - from: "src/tabs/GamesTab.jsx"
      to: "src/firebase.js"
      via: "createGame() and listGames() calls"
      pattern: "createGame|listGames"
    - from: "src/App.jsx"
      to: "/games/:id route"
      via: "Route element"
      pattern: "games/:id"
---

<objective>
Expand shared constants for the full position-aware stat set, add listGames() and replaceGameEvents() to firebase.js, build the GamesTab with game list + GameSetupModal, and wire the /games/:id route in App.jsx so LiveGameScreen (Plan 05-02) has a route to mount on.

Purpose: This plan provides the entry point for the entire game-day experience — creating a game and navigating to it — plus the expanded stat constants that Plans 05-03 and 05-04 depend on.
Output: Working game creation flow (modal -> Firestore write -> navigate to game route), game list on the Games tab, and expanded constants.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-game/05-CONTEXT.md
@.planning/phases/05-live-game/05-RESEARCH.md

@src/shared/constants.js
@src/firebase.js
@src/tabs/GamesTab.jsx
@src/App.jsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/firebase.js (existing game CRUD):
```javascript
export async function createGame({ opponent, date, lineup }) → Promise<string|null>
export async function loadGame(gameId) → Promise<Object|null>
export async function updateGameStatus(gameId, status) → Promise<boolean>
export async function updateGameScore(gameId, score) → Promise<boolean>
export async function appendGameEvent(gameId, event) → Promise<boolean>
```

From src/firebase.js (existing imports already available):
```javascript
import { getDocs, query, orderBy } from "firebase/firestore"; // ADD getDocs, query, orderBy to imports
const gamesCol = collection(db, "games"); // Already exists
```

From src/shared/constants.js (existing):
```javascript
export const POSITION_GROUP = { GK: "GK", DEF: "DEF", MID: "MID", FWD: "FWD" };
export const C = { statOffensive: "#E86420", statDefensive: "#4CAFB6", statNeutral: "#6b7280", ... };
```

From src/App.jsx (existing structure):
```javascript
// HashRouter with Routes: /lineup, /games, /stats
// TabBar component with fixed bottom nav
// paddingBottom: 56 wrapper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand constants + add listGames() + replaceGameEvents() + getPositionGroup()</name>
  <files>src/shared/constants.js, src/firebase.js, src/shared/utils.js</files>
  <action>
1. In `src/shared/constants.js`, replace the existing `STAT_TYPES` and `STAT_COLORS` with the full position-aware set:
   - Add `POSITION_STATS` map: GK: ["save", "distribution", "clearance", "fifty_fifty"], DEF: ["tackle", "interception", "clearance", "block", "fifty_fifty"], MID: ["goal", "assist", "great_pass", "shot_on_target", "tackle", "interception", "fifty_fifty"], FWD: ["goal", "assist", "great_pass", "shot_on_target", "fifty_fifty"]
   - Update `STAT_COLORS` to cover all new stat names: goal/assist/great_pass/shot_on_target → statOffensive, save/tackle/interception/clearance/block → statDefensive, fifty_fifty/distribution → statNeutral
   - Add `STAT_LABELS` map that maps snake_case keys to display labels: fifty_fifty → "50/50 Won", great_pass → "Great Pass", shot_on_target → "Shot on Target", etc.
   - Keep existing `STAT_TYPES` (grouped by category) for backward compat but mark with comment "// Legacy — use POSITION_STATS for Phase 5+"

2. In `src/shared/utils.js`, add `getPositionGroup(label)` function:
   - GK → "GK"
   - LB, CB, RB, LCB, RCB → "DEF"
   - LM, CM, RM, LCM, RCM → "MID"
   - LS, RS, LW, CF, RW, ST → "FWD"
   - Default fallback → "MID"
   - Export the function

3. In `src/firebase.js`, add two new functions:

   a. `listGames()`:
      - Add `getDocs`, `query`, `orderBy` to the existing firebase/firestore import
      - `listGames()` queries gamesCol ordered by createdAt desc, returns array of {id, ...data} objects
      - Silent error handling (return [] on failure), matching existing pattern

   b. `replaceGameEvents(gameId, events)`:
      - Replaces the entire events array on a game document: `updateDoc(doc(db, "games", gameId), { events })`
      - Returns true on success, false on failure (same pattern as other CRUD functions)
      - This is needed by Plan 05-04 for undo functionality (arrayRemove doesn't work with object equality from arrayUnion)
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>POSITION_STATS has 4 position groups with correct stat arrays. STAT_COLORS covers all stat names. STAT_LABELS maps all stat names to display strings. getPositionGroup maps every formation label correctly. listGames() and replaceGameEvents() exist in firebase.js. Vite build passes.</done>
</task>

<task type="auto">
  <name>Task 2: GamesTab with game list, GameSetupModal, and /games/:id route</name>
  <files>src/tabs/GamesTab.jsx, src/App.jsx</files>
  <action>
1. Replace GamesTab.jsx stub with a full implementation:
   - On mount, call `listGames()` and display results as cards in a scrollable list
   - Each game card shows: opponent name, date, status badge (setup/1st-half/halftime/2nd-half/completed), and score (home - away)
   - Status badge color: in-progress statuses use C.orange, completed uses C.statDefensive (teal), setup uses C.statNeutral
   - Game cards use navy background with subtle border, consistent with app styling (C.navy, C.navyLight, fontBase)
   - "+ New Game" FAB-style button (fixed position, bottom-right, above tab bar) — orange circle with "+" text
   - Tapping "+ New Game" opens GameSetupModal

2. Build GameSetupModal as a component within GamesTab.jsx (not a separate file — keeps it simple):
   - Full-screen overlay on mobile, centered modal on desktop
   - Two fields: Opponent Name (text input, required), Date (date input, defaults to today's date formatted as YYYY-MM-DD)
   - "Create Game" button — calls `createGame({ opponent, date, lineup })` where lineup is the current published lineup from `loadPublishedLineup()`. Load published lineup on modal open to get current formation + positions.
   - On successful creation (returns gameId), navigate to `/games/${gameId}` using `useNavigate()`
   - "Cancel" button closes modal
   - Input styling: 16px font-size minimum (iOS zoom prevention per Phase 3 decision), dark background inputs matching app theme

3. In App.jsx:
   - Import a placeholder `LiveGameScreen` component (inline, just shows "Loading game..." with the gameId from useParams — Plan 05-02 will replace)
   - Add route: `<Route path="/games/:id" element={<LiveGameScreen />} />`
   - Conditionally hide TabBar when on `/games/:id` route — use `useLocation()` in App component to detect. When pathname matches `/games/` followed by an ID, do not render TabBar and remove the paddingBottom: 56 on the wrapper div. This reclaims vertical space for the game screen.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Games tab shows game list cards. "+ New Game" button opens modal. Modal creates game in Firestore and navigates to /games/:id. TabBar hides on game routes. Vite build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. Navigate to Games tab — see game list (empty initially, then showing created games)
2. Tap "+ New Game" — modal opens with opponent + date fields, date defaults to today
3. Fill in opponent name, tap "Create Game" — navigates to /games/:id, tab bar is hidden
4. Navigate back to Games tab — new game appears in the list as a card
5. Build succeeds: `npx vite build`
</verification>

<success_criteria>
- POSITION_STATS, expanded STAT_COLORS, STAT_LABELS exported from constants.js
- getPositionGroup() exported from utils.js and maps all formation labels correctly
- listGames() in firebase.js returns game documents sorted by createdAt desc
- replaceGameEvents() in firebase.js replaces the events array on a game document
- GamesTab displays game cards and has a "+ New Game" button
- GameSetupModal creates a game in Firestore with snapshotted lineup
- /games/:id route exists and hides TabBar
- Vite build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-game/05-01-SUMMARY.md`
</output>
