---
phase: 05-live-game
plan: 03
type: execute
wave: 4
depends_on: ["05-02"]
files_modified:
  - src/games/LiveGameScreen.jsx
  - src/shared/FieldPosition.jsx
  - src/shared/utils.js
autonomous: true
requirements: [SUB-01, SUB-02, SUB-03, SUB-04]

must_haves:
  truths:
    - "Dragging a bench player onto a field position during a game logs a substitution event"
    - "Dragging a field player to the bench during a game logs a substitution event"
    - "Each player's field circle shows their running minute count during active play"
    - "Bench chips show total accumulated game minutes"
    - "Minutes update live (every second) during active halves"
    - "Minutes correctly account for halftime gaps and stoppage time"
  artifacts:
    - path: "src/shared/utils.js"
      provides: "calcMinutes() pure function for interval intersection"
      exports: ["calcMinutes"]
    - path: "src/shared/FieldPosition.jsx"
      provides: "minuteDisplay prop rendering on field circles"
      contains: "minuteDisplay"
    - path: "src/games/LiveGameScreen.jsx"
      provides: "Drag-and-drop substitution handling during active game"
      contains: "playerIntervals"
  key_links:
    - from: "src/games/LiveGameScreen.jsx"
      to: "src/shared/utils.js"
      via: "calcMinutes() call for live minute display"
      pattern: "calcMinutes"
    - from: "src/games/LiveGameScreen.jsx"
      to: "src/firebase.js"
      via: "appendGameEvent() for sub events"
      pattern: "appendGameEvent"
    - from: "src/shared/FieldPosition.jsx"
      to: "minuteDisplay prop"
      via: "prop rendering"
      pattern: "minuteDisplay"
---

<objective>
Add substitution handling to the live game screen and per-player minute tracking. Dragging players on/off the field during an active game logs substitution events, and every player's circle shows their running minute count.

Purpose: Minute tracking is the coach's primary tool for fair rotation — "who's been in too long?" and "who hasn't played yet?" at a glance. Substitution logging creates the data trail that feeds post-game summaries.
Output: Working drag-and-drop subs during game, live minute display on field circles and bench chips, calcMinutes() utility.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-game/05-CONTEXT.md
@.planning/phases/05-live-game/05-RESEARCH.md
@.planning/phases/05-live-game/05-01-SUMMARY.md
@.planning/phases/05-live-game/05-02-SUMMARY.md

@src/games/LiveGameScreen.jsx
@src/shared/FieldPosition.jsx
@src/shared/utils.js
@src/MadeiraLineupPlanner.jsx

<interfaces>
<!-- From Plan 05-02 outputs -->

From src/games/LiveGameScreen.jsx (state shape):
```javascript
// State variables available for extension:
// fieldPositions: array — positions with assigned players
// benchPlayers: array — players currently on bench
// events: array — game events [{id, type, ...}]
// playerIntervals: { [playerId]: [{ inAt: timestamp, outAt: timestamp|null }] }
// halfIntervals: [{ startAt: timestamp, endAt: timestamp|null }]
// gameStatus: "setup"|"1st-half"|"halftime"|"2nd-half"|"completed"
// displaySeconds: number — current elapsed seconds in half
```

From src/MadeiraLineupPlanner.jsx (existing drag-and-drop patterns to reuse):
```javascript
// Desktop: HTML5 drag/drop with onDragStart/onDragOver/onDrop
// Mobile: useTouchDrag hook (touchstart/touchmove/touchend) with data-drop-id attributes
// Field positions: data-drop-id="field-N"
// Bench: data-drop-id="chipstrip"
// Ghost element via createPortal during touch drag
```

From src/shared/FieldPosition.jsx (current props):
```javascript
// pos, player, isHighlighted, onDragStart, onDragEnd, onDragOver, onDrop,
// onClick, onDoubleClick, compact, dragSource, idx, onTouchStart, onTouchMove,
// onTouchEnd, onTouchCancel, isTouchDragOver
```

From src/shared/utils.js (existing exports):
```javascript
export function abbreviateName(name) → string  // Already exists — available for import
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calcMinutes() pure function + FieldPosition minute display prop</name>
  <files>src/shared/utils.js, src/shared/FieldPosition.jsx, src/tests/utils.test.js</files>
  <behavior>
    - calcMinutes([], []) returns 0 (no intervals)
    - calcMinutes([{inAt: 0, outAt: 600000}], [{startAt: 0, endAt: 1500000}]) returns 10 (10 minutes on field during a 25-min half)
    - calcMinutes([{inAt: 0, outAt: null}], [{startAt: 0, endAt: null}]) returns a positive number (uses Date.now() for open intervals)
    - calcMinutes with halftime gap: player on for full game, two half intervals with a gap — minutes = sum of both halves, not including the gap
    - calcMinutes with substitution mid-half: player subbed out at 10min, subbed back in at 15min — returns 20min (10 + 10) for a 25min half
    - calcMinutes floors to whole minutes (7min 42sec = 7)
    - getPositionGroup maps GK/DEF/MID/FWD labels correctly (populate existing todo stubs in utils.test.js)
  </behavior>
  <action>
1. In `src/tests/utils.test.js`, replace the `it.todo` stubs with real test implementations:
   - Import `calcMinutes` and `getPositionGroup` from `../../shared/utils.js`
   - Implement all calcMinutes test cases from the behavior block above
   - Implement all getPositionGroup test cases (GK, DEF labels, MID labels, FWD labels, default fallback)

2. In `src/shared/utils.js`, add the `calcMinutes(fieldIntervals, halfIntervals)` function:
   - For each field interval, for each half interval, compute the overlap: max(fi.inAt, hi.startAt) to min(fi.outAt ?? Date.now(), hi.endAt ?? Date.now())
   - Sum all positive overlaps in milliseconds
   - Return Math.floor(totalMs / 60000)
   - This is a pure function — no side effects, easy to test

3. In `src/shared/FieldPosition.jsx`:
   - Add `minuteDisplay` prop (string|null, e.g., "12" or null)
   - Add `isSelected` prop (boolean — for stat selection highlight in Plan 05-04, wire now so it doesn't need another edit)
   - Add `statCount` prop (number — for stat badge in Plan 05-04, wire now)
   - Render minuteDisplay below the player name: small text "Xm" style, color rgba(255,255,255,0.7), fontSize 9
   - Render statCount as a badge (small orange circle, position absolute top-right of the circle) if > 0
   - When isSelected is true, add a brighter border glow (e.g., 3px solid C.orange instead of 2.5px, stronger boxShadow)
   - All new props are optional — default to null/false/0 so existing usage in MadeiraLineupPlanner is unaffected
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vitest run src/tests/utils.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>calcMinutes() correctly computes interval intersections with whole-minute floor. getPositionGroup() passes all test cases. FieldPosition renders minuteDisplay, statCount badge, and isSelected highlight without breaking existing lineup builder usage. All utils tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Drag-and-drop substitutions during active game + live minute display</name>
  <files>src/games/LiveGameScreen.jsx</files>
  <action>
1. Add drag-and-drop handling to LiveGameScreen for substitutions during active game (status "1st-half" or "2nd-half"):
   - Reuse the same drag-and-drop patterns from MadeiraLineupPlanner.jsx:
     - Desktop: HTML5 drag events (onDragStart, onDragOver, onDrop) on FieldPosition and bench chips
     - Mobile: implement touch drag handling using the same touchstart/touchmove/touchend pattern with data-drop-id detection. Reference MadeiraLineupPlanner.jsx for the exact touch implementation — do NOT create a separate useTouchDrag hook, implement inline as in the planner.
   - When a bench-to-field or field-to-bench drag completes during an active game:
     a. Update fieldPositions and benchPlayers state (swap players)
     b. Log a substitution event: `{ id: crypto.randomUUID(), type: "sub", playerOut: {id, name, num}, playerIn: {id, name, num}, half: currentHalf, t: Date.now() }`
     c. Update playerIntervals: close the outgoing player's interval (set outAt = Date.now()), open a new interval for the incoming player (push { inAt: Date.now(), outAt: null })
     d. Append event to local events array
     e. Call `appendGameEvent(gameId, event)` to persist to Firestore (fire-and-forget, do not await before updating UI)
     f. Save updated state to localStorage immediately
   - Field-to-field swaps during game: just swap positions, no sub event (both players stay on field)
   - During halftime: allow bench-to-field and field-to-bench drags for pre-positioning, but do NOT log sub events (sub tracking pauses during halftime; intervals are re-opened when 2nd half starts for whoever is on field)
   - During "setup" or "completed": no drag interaction on game screen

2. Live minute display:
   - Use a `useMemo` (or compute in render) that calls `calcMinutes(playerIntervals[playerId], halfIntervals)` for each player
   - Pass the result as `minuteDisplay` prop to each FieldPosition: format as string like "12" (just the number, "m" suffix rendered by FieldPosition)
   - Actually — FieldPosition should render the "m" suffix. Update FieldPosition minuteDisplay rendering to show `{minuteDisplay}m` instead of just `{minuteDisplay}`
   - For bench chips: show minute count as well (e.g., "12m" text on the chip) so coach can see who's been sitting
   - Minutes update live: the rAF loop already updates `displaySeconds` every frame. Add a `displayMinute` state (Math.floor(displaySeconds / 60)) that only updates when the minute changes (avoid re-rendering 60x/sec). When `displayMinute` changes, trigger a re-render that recalculates all player minutes via calcMinutes.
   - During halftime and completed: show final accumulated minutes (no live updates)

3. Mirror all new state (playerIntervals, fieldPositions, benchPlayers) to localStorage via useEffect, consistent with existing crash buffer pattern from Plan 05-02.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Dragging players during active game logs sub events to local state and Firestore. Player intervals update correctly on sub. Field circles show live running minutes. Bench chips show accumulated minutes. Minutes survive halftime and crash recovery. Vite build succeeds.</done>
</task>

</tasks>

<verification>
1. Start a game, verify all field players show "0m" initially
2. After 1-2 minutes, verify minute counts increment on field circles
3. Drag a bench player onto a field player — see swap happen, sub event logged
4. Verify the subbed-out player's bench chip shows their accumulated minutes
5. Verify the subbed-in player's field circle starts counting from 0
6. End first half — verify halftime shows accumulated minutes
7. Start second half — verify minutes continue from where they left off for players who stayed on
8. Reload mid-game — verify minutes restore correctly
</verification>

<success_criteria>
- calcMinutes() correctly intersects player intervals with half intervals
- All utils tests pass (npx vitest run)
- Bench-to-field and field-to-bench drags during active game log sub events
- Sub events include playerIn, playerOut, half, timestamp, and UUID
- Player intervals (inAt/outAt) update on every substitution
- Field circles show live running minutes (updates every minute, not every frame)
- Bench chips show total accumulated minutes
- Minutes account for halftime gap correctly
- All sub-related state mirrors to localStorage
- Vite build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-game/05-03-SUMMARY.md`
</output>
