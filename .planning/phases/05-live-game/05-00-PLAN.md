---
phase: 05-live-game
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/tests/utils.test.js
  - src/tests/firebase.test.js
  - src/tests/timer.test.js
autonomous: true
requirements: [GAME-01, GAME-04, SUB-04, STAT-02, STAT-06]

must_haves:
  truths:
    - "vitest is installed and npx vitest run exits cleanly"
    - "utils.test.js has test stubs for calcMinutes, getPositionGroup, and undo event filtering"
    - "firebase.test.js has test stubs for createGame and listGames return shapes"
    - "timer.test.js has test stubs for drift-proof elapsed calculation"
  artifacts:
    - path: "package.json"
      provides: "vitest devDependency"
      contains: "vitest"
    - path: "src/tests/utils.test.js"
      provides: "Test stubs for calcMinutes, getPositionGroup, undo logic"
      min_lines: 20
    - path: "src/tests/firebase.test.js"
      provides: "Test stubs for createGame/listGames shape"
      min_lines: 10
    - path: "src/tests/timer.test.js"
      provides: "Test stubs for timer drift-proof logic"
      min_lines: 10
  key_links:
    - from: "src/tests/utils.test.js"
      to: "src/shared/utils.js"
      via: "import { calcMinutes, getPositionGroup }"
      pattern: "calcMinutes|getPositionGroup"
---

<objective>
Install vitest and create test file stubs that later plans will populate with real implementations. This is the Wave 0 test infrastructure that VALIDATION.md requires before any feature plans execute.

Purpose: Establish test infrastructure so every subsequent plan has automated verification beyond just "vite build passes."
Output: vitest installed, three test files with pending stubs ready for implementation in Plans 01-04.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-game/05-CONTEXT.md
@.planning/phases/05-live-game/05-VALIDATION.md

@package.json
@src/shared/utils.js
@src/firebase.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create test file stubs</name>
  <files>package.json, src/tests/utils.test.js, src/tests/firebase.test.js, src/tests/timer.test.js</files>
  <action>
1. Run `npm install --save-dev vitest` to add vitest to devDependencies.

2. Create `src/tests/utils.test.js` with the following test stubs (use `it.todo` for stubs that will be implemented by later plans):
   ```javascript
   import { describe, it, expect } from "vitest";

   describe("getPositionGroup", () => {
     it.todo("maps GK label to GK group");
     it.todo("maps LB, CB, RB, LCB, RCB to DEF");
     it.todo("maps LM, CM, RM, LCM, RCM to MID");
     it.todo("maps LS, RS, LW, CF, RW, ST to FWD");
     it.todo("defaults unknown labels to MID");
   });

   describe("calcMinutes", () => {
     it.todo("returns 0 for empty intervals");
     it.todo("calculates minutes for single interval within one half");
     it.todo("uses Date.now() for open intervals");
     it.todo("accounts for halftime gap between two halves");
     it.todo("accounts for substitution mid-half");
     it.todo("floors to whole minutes");
   });

   describe("undo event filtering", () => {
     it.todo("removes event by id from events array");
   });
   ```

3. Create `src/tests/firebase.test.js`:
   ```javascript
   import { describe, it, expect } from "vitest";

   describe("createGame shape", () => {
     it.todo("returns a string id on success");
     it.todo("returns null on failure");
   });

   describe("listGames shape", () => {
     it.todo("returns an array");
     it.todo("each item has id, opponent, date, status fields");
   });
   ```

4. Create `src/tests/timer.test.js`:
   ```javascript
   import { describe, it, expect } from "vitest";

   describe("timer elapsed calculation", () => {
     it.todo("computes elapsed seconds from Date.now() diff");
     it.todo("shows countdown when elapsed < 1500");
     it.todo("shows stoppage time when elapsed >= 1500");
     it.todo("formats display as MM:SS");
   });
   ```

5. Verify vitest runs cleanly: `npx vitest run` should exit 0 with all tests shown as todo/skipped.
  </action>
  <verify>
    <automated>cd /Users/jimrodarte/madeira-lineup && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>vitest installed in devDependencies. Three test files exist with todo stubs covering calcMinutes, getPositionGroup, undo filtering, createGame/listGames shapes, and timer elapsed logic. npx vitest run exits cleanly.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` exits with code 0
2. All three test files are listed in vitest output
3. vitest appears in package.json devDependencies
</verification>

<success_criteria>
- vitest is in devDependencies
- src/tests/utils.test.js exists with stubs for calcMinutes, getPositionGroup, undo
- src/tests/firebase.test.js exists with stubs for createGame/listGames shapes
- src/tests/timer.test.js exists with stubs for timer elapsed calculation
- npx vitest run exits cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-game/05-00-SUMMARY.md`
</output>
