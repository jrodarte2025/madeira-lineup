---
phase: 06-post-game-summary-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/summaryUtils.js
  - src/tests/summary.test.js
  - src/firebase.js
  - src/games/LiveGameScreen.jsx
  - src/tabs/GamesTab.jsx
  - src/App.jsx
  - package.json
autonomous: true
requirements: [POST-01, POST-02, POST-03, POST-04, POST-05]

must_haves:
  truths:
    - "Summary utility functions correctly compute per-player stats, minutes, active columns, and team totals from a game document"
    - "CSV builder produces valid CSV with proper escaping and correct column headers"
    - "MVP computation returns top 3 players by total stat events with correct abbreviations"
    - "finalizeGame writes playerIntervals and halfIntervals to Firestore before localStorage is cleared"
    - "End Game navigates the coach to /games/:id/summary"
    - "Tapping a completed game in the Games tab navigates to /games/:id/summary"
    - "The /games/:id/summary route exists and renders without error"
    - "html-to-image is installed and importable"
  artifacts:
    - path: "src/shared/summaryUtils.js"
      provides: "buildSummaryRows, buildCSV, getTopMVPs, formatMVPStats pure functions"
      exports: ["buildSummaryRows", "buildCSV", "getTopMVPs", "formatMVPStats"]
    - path: "src/tests/summary.test.js"
      provides: "Unit tests covering all summary utility functions"
      min_lines: 80
    - path: "src/firebase.js"
      provides: "finalizeGame function that writes intervals + sets status to completed"
      exports: ["finalizeGame"]
    - path: "src/games/GameSummaryScreen.jsx"
      provides: "Placeholder component at new route"
  key_links:
    - from: "src/games/LiveGameScreen.jsx"
      to: "src/firebase.js"
      via: "finalizeGame() call in handleEndGame"
      pattern: "finalizeGame\\(gameId"
    - from: "src/games/LiveGameScreen.jsx"
      to: "/games/:id/summary"
      via: "navigate() after finalizeGame resolves"
      pattern: "navigate.*summary"
    - from: "src/tabs/GamesTab.jsx"
      to: "/games/:id/summary"
      via: "onClick for completed games"
      pattern: "navigate.*summary"
    - from: "src/App.jsx"
      to: "src/games/GameSummaryScreen.jsx"
      via: "Route element"
      pattern: "Route.*summary.*GameSummaryScreen"
---

<objective>
Build the data layer, utility functions, and routing foundation for the post-game summary screen.

Purpose: All summary computation is pure-function logic that can be tested without DOM rendering. Getting this right first means the summary screen (Plan 02) can focus on rendering and export UX without implementing data logic. Also fixes the critical interval persistence gap and wires routing.

Output: Tested summary utilities, finalizeGame firebase function, route wiring, handleEndGame fix, GamesTab navigation fix, html-to-image installed.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-post-game-summary-exports/06-CONTEXT.md
@.planning/phases/06-post-game-summary-exports/06-RESEARCH.md

@src/shared/constants.js
@src/shared/utils.js
@src/firebase.js
@src/games/LiveGameScreen.jsx
@src/tabs/GamesTab.jsx
@src/App.jsx

<interfaces>
<!-- Key exports from existing codebase that this plan's tasks will use -->

From src/shared/constants.js:
```javascript
export const STAT_LABELS = {
  goal: "Goal", assist: "Assist", great_pass: "Great Pass",
  shot_on_target: "Shot on Target", save: "Save", tackle: "Tackle",
  interception: "Interception", clearance: "Clearance", block: "Block",
  fifty_fifty: "50/50 Won", distribution: "Distribution",
  shot: "Shot", foul: "Foul",
};
```

From src/shared/utils.js:
```javascript
export function calcMinutes(fieldIntervals, halfIntervals) { ... }
export function abbreviateName(name) { ... }
```

From src/firebase.js:
```javascript
export async function loadGame(gameId) { ... }
export async function updateGameStatus(gameId, status, halfStartTs = null) { ... }
// Game doc shape: { opponent, date, status, score: {home, away}, lineup, events: [], createdAt }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Summary utility functions with TDD tests</name>
  <files>src/shared/summaryUtils.js, src/tests/summary.test.js</files>
  <behavior>
    - buildSummaryRows: given a game doc with events array, lineup.roster, playerIntervals, halfIntervals — returns { rows, activeCols, totals }
    - buildSummaryRows: rows sorted by minutes descending, each row has { player, mins, stats }
    - buildSummaryRows: players with 0 minutes and 0 stats still appear in rows
    - buildSummaryRows: activeCols only contains stat types that have at least one recorded event
    - buildSummaryRows: totals.stats sums each active column across all players
    - buildCSV: produces header row "Player,Minutes,{stat columns}" followed by data rows and a TEAM TOTALS row
    - buildCSV: escapes player names containing commas or quotes via JSON.stringify
    - getTopMVPs: returns top 3 players by total stat event count; ties broken alphabetically by name
    - getTopMVPs: filters out players with 0 total stat events
    - getTopMVPs: handles fewer than 3 players with stats gracefully
    - formatMVPStats: produces abbreviated stat string like "2G 1A 1SOT"
    - formatMVPStats: omits stats with 0 count
  </behavior>
  <action>
    RED phase: Create src/tests/summary.test.js with tests for all behaviors listed above. Use vitest. Create mock game data with a roster of 4-5 players, events array with stat events, playerIntervals and halfIntervals. Run tests — all must fail.

    GREEN phase: Create src/shared/summaryUtils.js with these exports:
    - `buildSummaryRows(game)` — takes full game doc object. Iterates events to build per-player stat counts, derives active columns, computes minutes via `calcMinutes` from utils.js, builds rows sorted by minutes desc, computes team totals.
    - `buildCSV(rows, activeCols, game)` — takes rows/activeCols from buildSummaryRows plus game doc for metadata. Returns CSV string with proper escaping. Columns: Player, Minutes, then each active stat column using STAT_LABELS for header names. Ends with TEAM TOTALS row. File naming not here — this just returns the string.
    - `getTopMVPs(game, count=3)` — extracts stat counts from events, returns top `count` players sorted by total events desc then name asc. Each entry: { player, stats, total }.
    - `formatMVPStats(stats)` — takes a stats object { goal: 2, assist: 1 }, returns "2G 1A". Abbreviation map: goal=G, assist=A, shot_on_target=SOT, great_pass=GP, save=Sv, tackle=T, clearance=CL, block=Blk, interception=Int, fifty_fifty=50/50, distribution=Dist.

    Run tests — all must pass.
  </action>
  <verify>
    <automated>npx vitest run src/tests/summary.test.js</automated>
  </verify>
  <done>All summary utility tests pass. buildSummaryRows, buildCSV, getTopMVPs, formatMVPStats are exported and tested.</done>
</task>

<task type="auto">
  <name>Task 2: finalizeGame firebase function + handleEndGame fix + navigation wiring</name>
  <files>src/firebase.js, src/games/LiveGameScreen.jsx, src/tabs/GamesTab.jsx, src/App.jsx, src/games/GameSummaryScreen.jsx, package.json</files>
  <action>
    1. Install html-to-image: `npm install html-to-image`

    2. In src/firebase.js, add `finalizeGame(gameId, { playerIntervals, halfIntervals })`:
       - Uses `updateDoc` on games/{gameId} to write playerIntervals, halfIntervals, and status: "completed" in a single call
       - Returns true on success, false on error (matches existing silent error pattern)
       - Import updateDoc if not already imported

    3. In src/games/LiveGameScreen.jsx, fix handleEndGame:
       - CRITICAL: The current code calls setState for intervals then calls clearGameStorage — but the setState hasn't flushed yet. The intervals need to be computed inline before setState.
       - Compute closedHalfIntervals and closedPlayerIntervals inline (same logic as the current setState updaters but producing the final values directly)
       - Call `await finalizeGame(gameId, { playerIntervals: closedPlayerIntervals, halfIntervals: closedHalfIntervals })` BEFORE clearGameStorage
       - After finalizeGame resolves, call clearGameStorage() then navigate(`/games/${gameId}/summary`)
       - Import finalizeGame from firebase.js and useNavigate from react-router
       - Remove the existing `updateGameStatus(gameId, "completed")` call since finalizeGame handles status
       - Make handleEndGame async

    4. In src/tabs/GamesTab.jsx, update completed game navigation:
       - Change the GameCard onClick for completed games to navigate to `/games/${game.id}/summary`
       - Non-completed games still navigate to `/games/${game.id}` (the live game screen)
       - Update: `onClick={() => navigate(game.status === "completed" ? \`/games/${game.id}/summary\` : \`/games/${game.id}\`)}`

    5. Create src/games/GameSummaryScreen.jsx as a placeholder:
       - Import useParams from react-router
       - Extract gameId from params
       - Render a centered div with "Loading summary..." text using C.navy background and white text
       - This placeholder will be fully replaced in Plan 02

    6. In src/App.jsx:
       - Import GameSummaryScreen
       - Add route: `<Route path="/games/:id/summary" element={<GameSummaryScreen />} />`
       - Place it BEFORE the `/games/:id` route so it matches first
       - The existing isGameScreen regex `/^\/games\/.+/` already matches /games/:id/summary — TabBar will be hidden automatically
  </action>
  <verify>
    <automated>npx vitest run && node -e "import('html-to-image').then(() => console.log('ok')).catch(e => { console.error(e); process.exit(1) })"</automated>
  </verify>
  <done>finalizeGame exists in firebase.js. handleEndGame persists intervals to Firestore before clearing localStorage, then navigates to summary. Completed games in GamesTab navigate to summary route. Summary route exists with placeholder. html-to-image is installed.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/tests/summary.test.js` — all summary utility tests pass
- `npx vitest run` — full test suite passes (no regressions)
- `node -e "import('html-to-image').then(() => console.log('ok'))"` — html-to-image importable
- grep confirms: finalizeGame exported from firebase.js, imported in LiveGameScreen.jsx
- grep confirms: `/games/:id/summary` route in App.jsx
- grep confirms: navigate to summary in handleEndGame
- grep confirms: completed game navigation in GamesTab
</verification>

<success_criteria>
- Summary utility functions tested and exported
- Game finalization persists intervals to Firestore atomically with status change
- End Game flow: intervals persisted -> localStorage cleared -> navigate to summary
- Completed games navigate to summary from Games tab
- Summary route renders placeholder
- html-to-image installed
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-game-summary-exports/06-01-SUMMARY.md`
</output>
