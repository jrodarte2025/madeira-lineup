---
phase: 11-second-deployment-docs
plan: 01
type: execute
wave: 1
depends_on:
  - "10-01"
autonomous: partial
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04

scope: |
  Phase 11 = two parts.
    (A) Code/config work Claude can do autonomously:
        - Add `friend` alias to .firebaserc (points to placeholder project id
          until Jim creates the real one)
        - Add `deploy` and `deploy:friend` npm scripts that chain build +
          firebase deploy --only hosting
        - Write DEPLOYMENT.md documenting the full spin-up workflow for
          a new deployment (Madeira already ships; friend is the second
          instance; third+ follow the same docs)
    (B) Live deploy actions that require Jim (Firebase Console access +
        firebase CLI auth):
        - Create the friend's Firebase project in the console
        - Enable Firestore + Hosting
        - Copy the web-app credentials into .env.friend.local (replacing
          the Phase-8 placeholder values)
        - Update .firebaserc "friend" alias with the real project id
        - Run `npm run deploy:friend` once
  Part A lands in this plan. Part B is a documented runbook for Jim to
  execute when he's ready to onboard the friend's team live.
---

<tasks>
<task type="auto">
  <name>Task 1: .firebaserc + npm deploy scripts</name>
  <files>.firebaserc, package.json</files>
  <action>
    Update `.firebaserc`:
    - Add `madeira` alias pointing to `madeira-fc-lineups` (explicit —
      matches current default)
    - Add `friend` alias as placeholder (`YOUR-FRIEND-PROJECT-ID`) with
      a doc comment explaining replacement
    - Keep `default: madeira-fc-lineups` so Jim's existing workflow
      (`firebase deploy`) continues to target Madeira

    Update `package.json` scripts:
    - `deploy`: `npm run build && firebase deploy --only hosting`
      (targets whichever project is currently `firebase use`d — typically
      madeira when default is madeira)
    - `deploy:friend`: `npm run build:friend && firebase deploy --only
      hosting` (Jim must `firebase use friend` first)
    - `deploy:rules`: `firebase deploy --only firestore:rules` (helper
      for pushing rule changes independently of hosting bundles)
  </action>
</task>

<task type="auto">
  <name>Task 2: DEPLOYMENT.md runbook</name>
  <files>DEPLOYMENT.md</files>
  <action>
    Create `DEPLOYMENT.md` at repo root with sections:
    1. Overview — what a deployment is, what gets provisioned per instance
    2. Prerequisites — firebase CLI, Node/npm, repo clone
    3. Spin up a new deployment (step-by-step):
       a. Create Firebase project in console
       b. Enable Firestore (open rules, regional), Hosting
       c. Register web app → get credentials
       d. Add to this repo:
          - src/deployments/{id}.js with ROSTER + ALLOWED_FORMATION_KEYS
          - Register in src/config.js DEPLOYMENTS registry
          - Add alias to .firebaserc
          - Copy .env.friend.local.example → .env.{id}.local, fill in values
          - Add build:{id} / deploy:{id} npm scripts (pattern already
            established for friend)
       e. First deploy: npm run deploy:{id}
       f. Smoke test: open hosting URL, verify team name + roster render
    4. Deploy an existing deployment (friend):
       - Jim's runbook for when he's ready to ship friend's Firebase project
       - Already-created friend deployment: just replace placeholders and deploy
    5. Operate — push hosting updates, push rule changes, environment
       variables, rollback
    6. Known limitations — logo filename hardcoded, PWA manifest
       hardcoded, localStorage key prefix shared on same origin
    7. Troubleshooting
  </action>
</task>

<task type="auto">
  <name>Task 3: Final regression sweep (autonomous part)</name>
  <files>(none — verification only)</files>
  <action>
    - Full test suite (`npx vitest run`) must pass
    - Madeira build + dist/ bundle grep: contains Madeira strings, no
      friend leakage
    - Friend build + dist/ bundle grep: contains friend strings + quarters
      + 7v7 formations, no Madeira Firebase leakage
    - Restore dist/ to Madeira at end
    - Update STATE.md marking v3.0 milestone code-complete
  </action>
</task>

<task type="checkpoint:human-verify" gate="blocking-phase-11b">
  <name>Task 4: Jim's live-deploy runbook (blocking Phase 11b)</name>
  <action>
    This task is blocked on Jim creating the Firebase project for his
    friend's team. Runbook in DEPLOYMENT.md. Once done:
    - Jim verifies friend's hosting URL loads Friend FC correctly
    - Jim verifies Madeira's URL still loads Madeira correctly
    - Jim (or the coach) walks a quarters game end-to-end (build lineup,
      start Q1, let clock run or advance manually, log stats, end Q1,
      start Q2, halftime, Q3, Q4, Full Time!, summary, season dashboard)
    - No data cross-contamination between projects
  </action>
</task>
</tasks>
