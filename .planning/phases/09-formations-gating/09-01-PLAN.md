---
phase: 09-formations-gating
plan: 01
type: execute
wave: 1
depends_on:
  - "08-04"
files_modified:
  - src/shared/formations.js
  - src/deployments/friend.js
  - src/tests/deployment-resolver.test.js
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03

must_haves:
  truths:
    - "src/shared/formations.js FORMATIONS object contains 7 keys: 3-3-2, 3-2-3, 2-3-3, 4-3-1 (9v9, existing) + 2-3-1, 3-2-1, 2-2-2 (7v7, new)"
    - "Each 7v7 formation has exactly 7 position entries (1 GK + 6 outfield)"
    - "src/deployments/friend.js ALLOWED_FORMATION_KEYS is [\"2-3-1\", \"3-2-1\", \"2-2-2\"] (7v7 only — no 9v9)"
    - "src/deployments/madeira.js ALLOWED_FORMATION_KEYS is unchanged — still the 4 9v9 keys"
    - "Madeira bundle picker UI shows exactly 4 formations (the 9v9 set) — no 7v7 leakage"
    - "Friend bundle picker UI shows exactly 3 formations (the 7v7 set) — no 9v9 leakage"
---

<objective>
Add a 7v7 starter set (2-3-1, 3-2-1, 2-2-2) to the formation library and swap the friend deployment's allowlist to the 7v7 set. Madeira's allowlist stays unchanged. This proves the Phase 8 allowlist gating actually gates — the picker UI driven by ALLOWED_FORMATIONS now diverges between the two bundles.

Satisfies FORM-01 (7v7 formations exist), FORM-02 (Madeira shows only 9v9), FORM-03 (friend shows only 7v7).
</objective>

<context>
Current state from 08-04:
- src/shared/formations.js has 4 formations (all 9v9): 3-3-2, 3-2-3, 2-3-3, 4-3-1
- src/deployments/madeira.js ALLOWED_FORMATION_KEYS = ["3-3-2", "3-2-3", "2-3-3", "4-3-1"]
- src/deployments/friend.js ALLOWED_FORMATION_KEYS = ["3-3-2", "3-2-3", "2-3-3", "4-3-1"] (Phase 8 placeholder)

Coordinate system reference (from existing formations):
- Field is 100x100 % units; (0,0) = top-left, (100,100) = bottom-right
- GK is always at (50, 89) — top of own box at bottom of pitch
- Defenders live y=68-76, midfielders y=50-55, forwards y=27-30
- Single-center positions use x=50; pairs use x=36/64; triples use x=22/50/78 or 20/50/80

7v7 formations to add (derived from 9v9 spacing for visual consistency):

**2-3-1** — 2 defenders, 3 midfielders, 1 striker
  GK  (50, 89), LB (28, 70), RB (72, 70),
  LM  (22, 50), CM (50, 54), RM (78, 50),
  ST  (50, 28)

**3-2-1** — 3 defenders, 2 midfielders, 1 striker
  GK  (50, 89), LB (22, 73), CB (50, 76), RB (78, 73),
  LCM (36, 52), RCM (64, 52),
  ST  (50, 28)

**2-2-2** — 2 defenders, 2 midfielders, 2 forwards
  GK  (50, 89), LB (28, 72), RB (72, 72),
  LCM (36, 52), RCM (64, 52),
  LS  (36, 28), RS (64, 28)
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add 3 7v7 formations to the library</name>
  <files>src/shared/formations.js</files>
  <action>
    Append three new entries to the FORMATIONS object after the existing 4-3-1 entry. Keep the same formatting (same indentation, one key per formation).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Swap friend's allowlist to the 7v7 set</name>
  <files>src/deployments/friend.js</files>
  <action>
    Change `ALLOWED_FORMATION_KEYS` to `["2-3-1", "3-2-1", "2-2-2"]`. Update the top-of-file comment to note the swap.
  </action>
  <verify>
    <automated>npx vitest run src/tests/deployment-resolver.test.js</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update friend-resolver test to expect the 7v7 picker set</name>
  <files>src/tests/deployment-resolver.test.js</files>
  <action>
    Add/update the friend test to assert `Object.keys(ALLOWED_FORMATIONS)` equals the 7v7 set (sorted). Keep the Madeira assertion pointing at the 9v9 set.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Dual-build smoke — grep Madeira and friend bundles for picker contents</name>
  <files>(no source edits)</files>
  <action>
    Build both bundles. Grep each:
    - Madeira should contain "3-3-2" / "3-2-3" / "2-3-3" / "4-3-1" keys but NOT "2-3-1" / "3-2-1" (the only pair that doesn't collide with a 9v9 substring) as bare keys in the picker allowlist. (Note: "2-2-2" is actually a possible substring in 9v9 coords — don't over-constrain this check.)
    - Friend should contain "2-3-1" / "3-2-1" / "2-2-2" and NOT "3-3-2" / "3-2-3" / "2-3-3" / "4-3-1" in the friend's allowlist surface.
    - Because the full library is imported in both bundles for back-compat saved-game rendering, the 7v7 formations' COORDINATES will appear in both bundles — we're checking picker ALLOWLIST keys, not library presence.
    - Restore dist/ to Madeira build at end.
  </action>
  <verify>
    <automated>rm -rf dist &amp;&amp; npm run build &amp;&amp; rm -rf dist &amp;&amp; npm run build:friend &amp;&amp; rm -rf dist &amp;&amp; npm run build</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- FORM-01: 3 new 7v7 formations in the library, each with 7 positions
- FORM-02: Madeira's picker shows exactly the 4 9v9 formations
- FORM-03: Friend's picker shows exactly the 3 7v7 formations
- Both bundles build clean, all tests pass, no cross-leakage
</success_criteria>

<output>
After completion, create `.planning/phases/09-formations-gating/09-01-SUMMARY.md`.
</output>
